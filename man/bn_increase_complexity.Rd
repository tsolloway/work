% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_increase_complexity.R
\name{bn_increase_complexity}
\alias{bn_increase_complexity}
\title{Build Layered Bayesian Network Whitelists with Tabu and Bootstrap}
\usage{
bn_increase_complexity(
  df,
  dv = NULL,
  white_list_base,
  black_list = NULL,
  dv_arcs = NULL,
  connections_max = 3,
  bootstrap_reps = 100,
  algorithm = c("tabu", "hc", "tree.bayes"),
  score = c("bic", "aic"),
  threshold = NULL,
  auto_threshold_ratio = 1/4,
  strength_min = 0.01,
  align_direction = TRUE,
  return_strength = FALSE,
  seed = 1,
  ...
)
}
\arguments{
\item{df}{Data frame of variables including the dependent variable.}

\item{dv}{Optional name of the dependent variable (column in \code{df}).}

\item{white_list_base}{Initial whitelist of arcs (data frame with \code{from} and \code{to}).}

\item{black_list}{Optional blacklist of arcs (data frame with \code{from} and \code{to}).}

\item{dv_arcs}{Optional arcs to exclude from the first layer (typically arcs involving DV).}

\item{connections_max}{Maximum number of parents per node (maxp) for iterative layers.}

\item{bootstrap_reps}{Number of bootstrap replicates for \code{bn_boot_strength()}.}

\item{algorithm}{Structure learning algorithm: \code{"tabu"}, \code{"hc"}, \code{"tree.bayes"}.}

\item{score}{Scoring function for structure learning: \code{"bic"} or \code{"aic"}.}

\item{threshold}{Minimum strength to retain arcs or \code{"auto"}.}

\item{auto_threshold_ratio}{Proportion of arcs to retain if threshold = "auto".}

\item{strength_min}{Minimum bootstrap strength below which arcs are removed.}

\item{align_direction}{Logical; if TRUE, arcs are oriented to match majority direction.}

\item{return_strength}{Logical; if TRUE, include \code{strength} column in each layer output.}

\item{seed}{Optional integer for reproducibility.}

\item{...}{Additional arguments passed to \code{bn_boot_strength()}.}
}
\value{
A list of whitelists, one per layer. Each data frame optionally includes \code{strength}.
}
\description{
Iteratively builds layered whitelists of arcs using tabu search and
bootstrapped arc strength evaluation. Only bootstraps arcs that are new
in each layer to reduce redundant computation.
}

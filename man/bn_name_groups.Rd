% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_name_groups.R
\name{bn_name_groups}
\alias{bn_name_groups}
\title{bn_name_groups}
\usage{
bn_name_groups(
  results,
  manual_names = NULL,
  model = "claude-sonnet-4-20250514",
  max_words = 3,
  min_words = 1,
  api_key = NULL,
  verbose = TRUE
)
}
\arguments{
\item{results}{A BN results object from \code{\link[=bn_initial_networks]{bn_initial_networks()}} or
\code{\link[=bn_engine]{bn_engine()}}. Can be a single engine result, unsupervised
(\code{bn_initial_networks}), or supervised (\code{bn_initial_networks} with DVs).}

\item{manual_names}{Character vector or list of character vectors, or NULL.
When provided, skips the Claude API call and assigns these names directly.
For a single engine result, pass a character vector. For multiple results,
pass a list of character vectors in the same order as the results. Each
vector must match the number of communities in its corresponding engine.
Default NULL (use Claude).}

\item{model}{Character. Claude model ID (default \code{"claude-sonnet-4-20250514"}).}

\item{max_words}{Integer. Maximum words per group name (default 3).}

\item{min_words}{Integer. Minimum words per group name (default 1).}

\item{api_key}{Character or NULL. Anthropic API key. If NULL, reads from
\code{ANTHROPIC_API_KEY} environment variable.}

\item{verbose}{Logical. Print group naming results to console (default TRUE).}
}
\value{
The same \code{results} object with \code{community_name} updated in all
\code{viz_prep$attribute_viz_prep$nodes} data frames, and \code{community_viz_prep}
regenerated to reflect the new names.
}
\description{
Use Claude to generate descriptive names for community groups
in a Bayesian Network result. Replaces default "Group 1", "Group 2", etc.
with short thematic labels based on the node labels within each group.
Each engine result is named independently since different network types
may produce different community structures.
}

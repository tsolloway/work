% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_default_index_column.R
\name{bn_default_index_column}
\alias{bn_default_index_column}
\title{Resolve the Raw Metric Column a Drivers Dashboard Opens On}
\usage{
bn_default_index_column(
  col_names,
  subgroup = NULL,
  outcome_display = "absolute",
  shift_type = "absolute"
)
}
\arguments{
\item{col_names}{Character vector. Column names of the impact table.}

\item{subgroup}{Character or NULL. Subgroup whose columns to resolve, e.g.
\code{"Total"}. NULL treats \code{col_names} as already unprefixed.}

\item{outcome_display}{Character. \code{"absolute"} or
\code{"proportional"}.}

\item{shift_type}{Character. One of \code{"absolute"},
\code{"proportional"}, \code{"headroom"}, \code{"range"}. Ignored
whenever an Assess preset is available.}
}
\value{
A length-1 character column name, or \code{NA_character_} when no
candidate exists.
}
\description{
Internal helper. Given the column names of an impact table, returns the one
raw metric column the Attribute / Community Drivers dashboard displays on
first open for \code{subgroup}.
}
\details{
The workbook opens on the first Assess preset (normally "Current Impact" =
Average Effect + range shift), and a preset \strong{overrides} \code{shift_type} --
the Analysis and Shift Type cells are driven by Assess and greyed out. Only
when no preset can be built does the raw \code{shift_type} argument decide.

That override is why the network-map PNGs cannot reuse
\code{bn_impact_write()}'s \code{.resolve_metric_suffix()}: it models
neither the preset override nor the \code{headroom} / \code{range} shift
tags (it collapses both to \code{propshift}).

Kept as a single function so \code{append_bn_impact_dynamic()}'s row
pre-sort and \code{append_bn_network_maps()}'s dot sizing resolve the
opening view identically and cannot drift apart.
}
\keyword{internal}

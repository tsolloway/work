% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_recursive.R
\name{find_recursive}
\alias{find_recursive}
\title{Recursively Search Nested Lists for an Object by Class, Name, or Both}
\usage{
find_recursive(
  x,
  x_class = NULL,
  x_name = NULL,
  require_both = FALSE,
  max_depth = Inf,
  return_logical = FALSE,
  depth = 0
)
}
\arguments{
\item{x}{Object or list to search through.}

\item{x_class}{Optional character scalar. Class name to search for
(e.g., \code{"bn"}, \code{"bn.fit"}). If \code{NULL}, class is ignored.}

\item{x_name}{Optional character scalar. Element name to search for.
If \code{NULL}, name is ignored.}

\item{require_both}{Logical; if \code{TRUE}, both class and name must match
for an element to be returned. If \code{FALSE} (default), either condition
can match.}

\item{max_depth}{Positive integer or \code{Inf}. Maximum recursion depth.
Prevents infinite recursion in self-referential or excessively nested lists.
Defaults to \code{Inf}, meaning no limit.}

\item{return_logical}{Logical; if \code{TRUE}, returns \code{TRUE} if a match is found,
or \code{FALSE} otherwise, instead of returning the object itself.
Default = \code{FALSE}.}

\item{depth}{Internal parameter used during recursion (do not modify).}
}
\value{
By default, the first matching object found according to the specified
criteria, or \code{NULL} if none are found within the specified \code{max_depth}.

If \code{return_logical = TRUE}, returns \code{TRUE} if a match exists,
or \code{FALSE} otherwise.
}
\description{
Recursively searches through a (potentially deeply nested) list
to find the first element that either:
\itemize{
\item Inherits from a specified class (\code{x_class}),
\item Has a specified name (\code{x_name}),
\item Or satisfies both conditions.
}

Optionally, it can return a logical indicator (\code{TRUE} / \code{FALSE}) rather
than the object itself, which is useful for validation checks.
}
\details{
The function performs a \strong{depth-first search}, inspecting each list
element recursively until it finds one that matches the specified
criteria.
\itemize{
\item \strong{Search by class:} If \code{x_class} is provided, the function checks whether
each element inherits from that class via \code{inherits()}.
\item \strong{Search by name:} If \code{x_name} is provided, the function checks whether
the current list level contains an element with that name.
\item \strong{Combined search:} If both \code{x_class} and \code{x_name} are supplied, the argument
\code{require_both} determines whether both conditions must be true (\code{TRUE})
or if either suffices (\code{FALSE}).
\item \strong{Depth control:} The \code{max_depth} parameter limits recursion to prevent
infinite loops in cyclic or malformed structures. If it exceeds the actual
depth, the function behaves normally.
\item \strong{Logical output:} When \code{return_logical = TRUE}, the function returns
a logical flag instead of the object. This is useful in conditional
checks or validation pipelines.
\item \strong{Error safety:} Recursive calls are wrapped in \code{tryCatch()} to avoid
interruption by malformed or inaccessible elements.
}

If no match is found, the function returns either \code{NULL} or \code{FALSE},
depending on the value of \code{return_logical}.
}
\examples{
\dontrun{
library(bnlearn)
bn <- bnlearn::hc(iris)
nested <- list(a = list(b = list(model = bn)))

# Search by class only
find_recursive(nested, x_class = "bn")

# Search by name only
find_recursive(nested, x_name = "model")

# Search by both name and class (must satisfy both)
find_recursive(nested, x_class = "bn", x_name = "model", require_both = TRUE)

# Logical return mode
find_recursive(nested, x_class = "bn", return_logical = TRUE)
find_recursive(nested, x_class = "bn.fit", return_logical = TRUE)
}

}

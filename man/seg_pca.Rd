% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_pca.R
\name{seg_pca}
\alias{seg_pca}
\title{seg_pca}
\usage{
seg_pca(
  seg,
  vars = NULL,
  polar_type = c("rs", "source"),
  clean_max = 0.25,
  where = NULL,
  file_name = "Factor Analysis",
  rotation = c("equamax", "varimax", "quartimax", "bentlerT", "varimin", "geominT",
    "bifactor", "Promax", "promax", "oblimin", "simplimax", "bentlerQ", "geominQ",
    "biquartimin", "none"),
  return_object = FALSE,
  add_proj_name_to_file = TRUE
)
}
\arguments{
\item{seg}{A seg object with shell data populated.}

\item{vars}{Character vector of variable names to analyze. If \code{NULL}
(default), uses all RS polar variables from the spec.}

\item{clean_max}{Numeric. Loading threshold below which variables are
considered clean (default: \code{0.25}).}

\item{where}{Character. Output directory. Defaults to the process folder in
the seg object.}

\item{file_name}{Character. Base file name (default: \code{"Factor Analysis"}).}

\item{rotation}{Character. Rotation method passed to \code{\link[psych:principal]{psych::principal()}}
(default: \code{"equamax"}).}

\item{return_object}{Logical. If \code{TRUE}, stores the full PCA analysis object
in \code{seg[["pca_analysis"]]} (default: \code{FALSE}).}

\item{add_proj_name_to_file}{Logical. If \code{TRUE} (default), prepends the
project name to the file name.}
}
\value{
The seg object with \code{seg[["paths"]][["files"]][["pca"]]} set.
}
\description{
Runs principal components analysis on the polar shell variables,
writes the results to an Excel workbook, and stores the file path in the
seg object.
}

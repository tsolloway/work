% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_finalize_prep_solution.R
\name{seg_finalize_prep_solution}
\alias{seg_finalize_prep_solution}
\title{seg_finalize_prep_solution}
\usage{
seg_finalize_prep_solution(
  seg,
  solution,
  final_solution_name,
  overwrite = FALSE,
  method = c("sequential", "best_combo"),
  reduce_type = c("greedy_step", "greedy", "step"),
  priors = c("equal", "size"),
  remove_first = NULL,
  min_items = NULL,
  max_items = NULL,
  max_combos = 5000,
  seed = 1,
  segment_labels = NULL,
  segment_descriptions = NULL,
  segment_names = NULL,
  qualification_instructions = rep("qualification goes here", 8),
  survey_respondent_id = NULL,
  truncate = c("no", "yes", "both"),
  weighted = TRUE,
  version = c("traditional", "both"),
  strategy = c("multisession", "multicore", "sequential", "cluster"),
  workers = NULL,
  setting_polar_threshold = 0.2,
  setting_profile_threshold = 0.15,
  setting_diff = 0.1,
  setting_pvalue = 0.1
)
}
\arguments{
\item{seg}{A seg object with solutions in \code{seg$solutions$summary_table} and
\code{seg$data$with_solutions} populated.}

\item{solution}{Character. Name of the source solution to finalize. Matched
against \code{lda_name} first, then \code{solution_name}. Its full input set seeds
the short-form reduction.}

\item{final_solution_name}{Character. Base name for the finalized solution,
its output folder, and the bare full-size solution (e.g. \code{"Solution_A"}).}

\item{overwrite}{Logical. If \code{FALSE} (default) and \code{final_solution_name}
already exists as a folder or registered solution, the function stops. If
\code{TRUE}, the existing output folder and any previously finalized rows /
columns for this name are removed first.}

\item{method}{Character. How to reduce the item set: \code{"sequential"}
(default) runs a stepwise greedy backward reduction; \code{"best_combo"}
evaluates every variable combination at each item count and keeps the best
per size (slower, exhaustive).}

\item{reduce_type, priors, remove_first, min_items, max_items, max_combos, seed}{Passed through to \code{\link[=seg_short_form_analysis]{seg_short_form_analysis()}} to control the item
reduction. \code{reduce_type} and \code{remove_first} apply to \code{method = "sequential"}; \code{max_combos} applies to \code{method = "best_combo"}.}

\item{segment_labels, segment_descriptions}{Passed through to
\code{\link[=seg_write_short_form_analysis]{seg_write_short_form_analysis()}} for the analysis workbook's column
headers and legend.}

\item{segment_names, qualification_instructions}{Passed through to
\code{\link[=seg_typing_tool]{seg_typing_tool()}}.}

\item{survey_respondent_id}{Character. Respondent ID column for the typing
tool. Defaults to the seg object's respondent id (\code{\link[=get_resp_id_name]{get_resp_id_name()}}).}

\item{truncate, weighted, version, strategy, workers}{Passed through to
\code{\link[=seg_write_shell_parallel]{seg_write_shell_parallel()}} for the solution shells.}

\item{setting_polar_threshold, setting_profile_threshold}{Numeric (defaults
\code{0.2} / \code{0.15}). Significance/colouring thresholds for polar and profile
variables, passed through to \code{\link[=seg_write_shell_parallel]{seg_write_shell_parallel()}}.}

\item{setting_diff, setting_pvalue}{Numeric (defaults \code{0.1}). Seed the master
Diff / p-value thresholds in the \code{\link[=seg_compare_segments]{seg_compare_segments()}} workbook, which
is written into the output folder restricted to the finalized solutions.}
}
\value{
The seg object, invisibly, with the finalized solutions registered
under \code{seg$solutions$analysis[[final_solution_name]]} and \code{summary_table} /
\code{with_solutions} rebuilt.
}
\description{
Finalizes a chosen LDA solution into a complete, deliverable
bundle. Given a solution and a \code{final_solution_name}, this:
\enumerate{
\item runs \code{\link[=seg_short_form_analysis]{seg_short_form_analysis()}} to produce the full solution plus
every shorter item-count reduction down to \code{min_items};
\item registers each finalized solution on the seg object under a new
analysis family named \code{final_solution_name} (its rows also carry
\code{solution_name = final_solution_name}). The full (max-item) solution
takes the bare \code{final_solution_name}; each reduction is suffixed with
its item count
(e.g. \code{Solution_A}, \code{Solution_A_11}, \code{Solution_A_10}, ...,
\verb{Solution_A_\{min\}}). \code{Solution_A} is the short-form max-item refit row,
so all sizes are produced by the same pipeline and nest cleanly;
\item creates an output folder named \code{final_solution_name} under a
shared \code{FINAL-SOLUTIONS} folder in the project's solution folder
(i.e. \verb{3. Solutions/FINAL-SOLUTIONS/\{final_solution_name\}}) and writes
the short-form analysis workbook and a shell for every finalized
solution (in parallel) into it;
\item writes a typing tool for every finalized solution into a
\code{"{final_solution_name} - Typing Tools"} subfolder;
\item prints the source solution's LDA accuracy and reliability metrics.
}

If \code{final_solution_name} already exists — either as an output folder or as
a registered solution name — the function stops unless \code{overwrite = TRUE}.
}

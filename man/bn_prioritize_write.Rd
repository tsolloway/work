% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_prioritize_write.R
\name{bn_prioritize_write}
\alias{bn_prioritize_write}
\title{bn_prioritize_write}
\usage{
bn_prioritize_write(
  result,
  title = NULL,
  sub_title = NULL,
  file_name = NULL,
  variable_width = 20,
  community_width = 20,
  label_width = 20,
  combo_width = 40,
  sig_threshold = NULL,
  marginal_threshold = NULL,
  lift = 0.1,
  min_base_for_boot = NULL,
  very_hide_all = TRUE,
  path = ".",
  wb = NULL,
  save = TRUE,
  add_guide = TRUE,
  add_prioritization_pvalue = FALSE,
  prioritize_display = NULL
)
}
\arguments{
\item{result}{Output of \code{bn_prioritize()} (a single tibble) or
\code{bn_prioritizations()} (a list of results with meta).}

\item{title}{Character or NULL. Title displayed in the sheet header. If NULL,
defaults to \code{"Prioritization Analysis of {dv}"} using the DV display
name from \code{result$meta$dv}. Default NULL.}

\item{sub_title}{Character or NULL. Subtitle text displayed in the sheet
header (e.g. project name). If NULL, inherits from \code{file_name}.
Default NULL.}

\item{file_name}{Character or NULL. Prefix for output file name. If NULL,
inherits from \code{sub_title}. Default NULL.}

\item{variable_width}{Numeric. Column width for the Variable column.
Default 20.}

\item{community_width}{Numeric. Column width for the Community column.
Default 20.}

\item{label_width}{Numeric. Column width for the Label column. Default 20.}

\item{combo_width}{Numeric. Column width for the Combo column.
Default 40.}

\item{sig_threshold}{Numeric or NULL. P-value threshold for green
(significant). If NULL, inherits from \code{result$meta$sig_threshold}
(set at \code{bn_prioritizations()} / \code{bn_finalize_network()}
time); falls back to 0.05.}

\item{marginal_threshold}{Numeric or NULL. P-value threshold for orange
(marginal). If NULL, inherits from
\code{result$meta$marginal_threshold}; falls back to 0.10.}

\item{lift}{Numeric. The lift fraction used in the prioritization analysis,
displayed in the footer. Default 0.10 (10 percent).}

\item{min_base_for_boot}{Integer or NULL. Threshold used for the base /
warning feedback shown next to the Focus dropdown. When the current
selection's base is at or above this value, the cell reads
\code{"Base: N"}; below it the cell reads
\code{"Results not calculated because base is below N"} in red, and the Focus
dropdown cell itself turns red. If NULL, inherits from
\code{result$meta$min_base_for_boot} (falling back to 100).}

\item{very_hide_all}{Logical. If TRUE (default), all sheets except
Prioritization are set to veryHidden. If FALSE, they are simply hidden.}

\item{path}{Character. Directory to write workbook to. Default \code{"."}.}

\item{wb}{openxlsx workbook object or NULL. When NULL (default), a new
workbook is created. When provided, prioritization sheets are appended
to the existing workbook — used by \code{bn_write()}.}

\item{save}{Logical. When TRUE (default), the workbook is saved to disk.
When FALSE, the workbook is returned without saving.}

\item{add_guide}{Logical. When TRUE (default), a Guide tab is appended.
When FALSE, no Guide is added — used by \code{bn_write()} which builds
a single unified Guide for the combined workbook.}

\item{add_prioritization_pvalue}{Logical. When TRUE, the prioritization
table includes its p-value column (current behavior). When FALSE
(default), the p-value column is removed from the prioritization table.}

\item{prioritize_display}{Character or NULL. Initial value of the
prioritization Display dropdown — either \code{"Point Change"} or
\code{"\% Change"}. When NULL (default), the value is auto-detected from
the DV type: dichotomous outcomes default to \code{"Point Change"},
continuous outcomes default to \code{"\% Change"}. Pass an explicit string
to override the auto-detection.}
}
\value{
Workbook object (invisibly).
}
\description{
Takes the output of \code{bn_prioritize()} or
\code{bn_prioritizations()} and writes a formatted Excel workbook with
a dynamic dashboard. Dropdown controls for strategy, search, subgroup,
focus (brand), and weight switch between hidden data sheets. Controls
are only shown when multiple options exist for that dimension.
}
\seealso{
\code{\link[=bn_prioritize]{bn_prioritize()}}, \code{\link[=bn_prioritizations]{bn_prioritizations()}}
}

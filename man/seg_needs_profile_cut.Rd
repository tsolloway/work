% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_profile_cut.R
\name{seg_needs_profile_cut}
\alias{seg_needs_profile_cut}
\title{seg_needs_profile_cut}
\usage{
seg_needs_profile_cut(
  seg,
  cells = NULL,
  blocks = NULL,
  vars = NULL,
  exclude_basis = TRUE,
  top_n = 10,
  alpha = 0.05,
  min_n = 30
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_cut]{seg_needs_cut()}} and \code{\link[=seg_get_spec]{seg_get_spec()}}.}

\item{cells}{Cells to profile, as codes or labels. Default: every cell with
at least \code{min_n} respondents.}

\item{blocks}{Character. Spec block prefixes to profile against, e.g.
\code{c("DE", "HO")}. Default: every person-level block.}

\item{vars}{Character. Individual spec variables, added to \code{blocks}.}

\item{exclude_basis}{Logical. Leave out spec variables built from the needs
loop itself (default \code{TRUE}). A cell typed on the needs will of course
differ on the needs; those rows restate the typing and crowd out the
profile. Applies only when neither \code{blocks} nor \code{vars} is given.}

\item{top_n}{Integer. Distinguishing variables printed per cell
(default \code{10}).}

\item{alpha}{Numeric. Significance level for a variable to be printed
(default \code{0.05}).}

\item{min_n}{Integer. Smallest cell profiled by default (default \code{30}).}
}
\value{
The seg object, invisibly, with the full comparison in
\code{seg[["needs"]][["reports"]][["profile_cut"]]}. Prints the most
distinguishing variables per cell.
}
\description{
Profiles cells of the needs cut against the person-level spec
variables: what distinguishes the respondents in a cell from everyone else
who was classified. A quick read for deciding what a cell is, before the
full shell from \code{\link[=seg_needs_write_cut]{seg_needs_write_cut()}}.

Reads the executed spec at person level - one row per respondent, with
every column that varies across a respondent's contexts removed, the same
rule \code{\link[=seg_needs_write_shell]{seg_needs_write_shell()}} uses. Each cell is compared with the rest of
the classified respondents: means, difference, index (cell / total x 100)
and a Welch t-test p-value. Unweighted.
}

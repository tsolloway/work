% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_set_unit.R
\name{seg_needs_set_unit}
\alias{seg_needs_set_unit}
\title{seg_needs_set_unit}
\usage{
seg_needs_set_unit(seg, level = c("grid", "person"))
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_prepare]{seg_needs_prepare()}}.}

\item{level}{Character. \code{"grid"} (default) makes the person-context grid the
unit; \code{"person"} restores the raw respondent frame.}
}
\value{
The seg object with \code{seg$data$original} set to the chosen frame.
}
\description{
Sets which frame is the unit of analysis, i.e. which one the
spec executes against.

A needs project carries two frames: the person frame loaded by
\code{\link[=seg_get_data]{seg_get_data()}}, and the person-context grid built by
\code{\link[=seg_needs_stack]{seg_needs_stack()}}. Everything downstream of the spec - \code{\link[=seg_do_spec]{seg_do_spec()}},
\code{\link[=seg_write_shell]{seg_write_shell()}}, the cluster functions - reads \code{seg$data$original}, so
whichever frame should be the unit has to be promoted into that slot.

Call it after \code{\link[=seg_needs_prepare]{seg_needs_prepare()}}, since prepare adds the basis columns
to the stacked frame.

Promotion is reversible: the person frame is kept at \code{seg$data$person}, so
\code{level = "person"} puts it back exactly as \code{\link[=seg_get_data]{seg_get_data()}} loaded it -
raw, without the basis columns \code{\link[=seg_needs_prepare]{seg_needs_prepare()}} wrote onto the
stacked frame. That path is for re-stacking, not for person-level
analysis. For a person-level READ of an executed spec use
\code{\link[=seg_needs_write_shell]{seg_needs_write_shell()}} with \code{level = "person"}, which subsets the
executed frame and so keeps every derived column. A fresh \code{seg_uuid} is issued each time
because the row means something different - one per grid at grid level,
one per respondent at person level. \code{person_id} is untouched either way,
so a grid-level solution can always be rolled back up.
}

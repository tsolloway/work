% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_state_lock.R
\name{seg_needs_state_lock}
\alias{seg_needs_state_lock}
\title{seg_needs_state_lock}
\usage{
seg_needs_state_lock(seg, decisive_only = FALSE, top_contexts = NULL)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_type]{seg_needs_type()}} or \code{\link[=seg_needs_cluster]{seg_needs_cluster()}}.}

\item{decisive_only}{Logical. Use only grids that are not near-ties
(default \code{FALSE}).}

\item{top_contexts}{Integer. Contexts to list in the over-index table
(default: all).}
}
\value{
The seg object, invisibly, with the tables in
\code{seg[["needs"]][["reports"]][["state_lock"]]}. Prints a verdict.
}
\description{
Whether states belong to people or to contexts. Two readings,
on whatever typing is current - adopted states or themes - and, when
\code{\link[=seg_needs_cluster]{seg_needs_cluster()}} has been run, across every stored k so the lock can
inform the choice of k.

\describe{
\item{person lock}{take any two grids from the same respondent: how often
are they in the same state, against two baselines? \code{chance} is two
grids drawn at random from the sample. \code{context} is two grids drawn
from the same pair of contexts the respondent was asked about, so it
credits whatever the contexts alone would produce. The lock is the
kappa against the context baseline: 0 = no more consistent than their
contexts, 1 = always the same state.}
\item{state x context}{Cramer's V between state and context, and for
each context the state it over-indexes on most.}
}

Pair-based, so the baselines are exact expectations rather than a
reshuffle. A strong person lock with a weak context association is the
premise of a needs segmentation - two people doing the same thing want
different things, and each wants much the same thing wherever they are.
}

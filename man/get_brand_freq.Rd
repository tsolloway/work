% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_brand_freq.R
\name{get_brand_freq}
\alias{get_brand_freq}
\title{get_brand_freq}
\usage{
get_brand_freq(df, brand_col, subgroups = NULL)
}
\arguments{
\item{df}{Data frame containing the brand column and any subgroup indicator
columns (0/1).}

\item{brand_col}{Character. Name of the column in \code{df} holding brand
labels.}

\item{subgroups}{Character vector or NULL. Names of 0/1 indicator columns
defining subgroups. Default NULL (Total only).}
}
\value{
A tibble with columns \code{subgroup} (first column, character) and
one integer column per brand value. The first row is always "Total".
}
\description{
Tabulate brand frequency counts across subgroups. Returns a
tibble with one row per subgroup (plus a "Total" row), one column per
unique brand value, and counts in the cells.
}
\examples{
\dontrun{
# Total only (no subgroups)
get_brand_freq(df_impute, brand_col = "brand_name")
# # A tibble: 1 x 4
#   subgroup ChatGPT Gemini Perplexity
#   <chr>      <int>  <int>      <int>
# 1 Total        820    310        500

# With subgroups (0/1 indicator columns in df)
get_brand_freq(
  df_impute,
  brand_col = "brand_name",
  subgroups = c("Regular_Google_User", "Power_User")
)
# # A tibble: 3 x 4
#   subgroup            ChatGPT Gemini Perplexity
#   <chr>                 <int>  <int>      <int>
# 1 Total                   820    310        500
# 2 Regular_Google_User     340    150        213
# 3 Power_User              210     95        120
}

}

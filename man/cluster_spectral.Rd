% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_spectral.R
\name{cluster_spectral}
\alias{cluster_spectral}
\title{cluster_spectral}
\usage{
cluster_spectral(
  df,
  vars,
  vars_profiles,
  solution_name,
  solution_name_prefix = "spectral",
  resp_id_name = NULL,
  filter_name = NULL,
  n_min = 4,
  n_max = 7,
  reduced_inputs_max = NULL,
  priors = c("equal", "size"),
  iter_max = 1000,
  nstart = 10,
  lda_vars = NULL,
  lda_vars_profiles = NULL,
  seed = 1
)
}
\arguments{
\item{df}{A data frame containing shell data (the full dataset including
all respondents, not just filtered ones).}

\item{vars}{Character vector of input variable names to cluster on.}

\item{vars_profiles}{Character vector of profile variable names
corresponding to \code{vars} (binary polar indicators).}

\item{solution_name}{Character. Solution family identifier (e.g. \code{"A"}).}

\item{solution_name_prefix}{Character. Prefix for cluster names
(default: \code{"spectral"}). Combined with \code{solution_name} and k to form
names like \code{"spectral_A4"}, \code{"spectral_A5"}.}

\item{resp_id_name}{Character or \code{NULL}. Respondent ID column name.}

\item{filter_name}{Character or \code{NULL}. Column name containing a logical
filter vector. \code{NULL} uses all rows.}

\item{n_min}{Integer. Minimum number of clusters (default: \code{4}).}

\item{n_max}{Integer. Maximum number of clusters (default: \code{7}).}

\item{reduced_inputs_max}{Integer or \code{NULL}. Cap on the number of reduced
input variables. \code{NULL} uses all selected variables.}

\item{priors}{Character. Prior probability method for LDA: \code{"equal"} or
\code{"size"} (default: \code{"equal"}).}

\item{iter_max}{Integer. Not used by specc but kept for signature
compatibility with other method wrappers.}

\item{nstart}{Integer. Not used by specc but kept for signature
compatibility with other method wrappers.}

\item{lda_vars}{Character vector or \code{NULL}. Override which input variables
are used for LDA. \code{NULL} uses all \code{vars}.}

\item{lda_vars_profiles}{Character vector or \code{NULL}. Override which profile
variables are used for LDA. \code{NULL} uses all \code{vars_profiles}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility
(default: \code{1}).}
}
\value{
A list with two elements:
\describe{
\item{all_inputs}{Tibble with one row per k (\code{n_min:n_max}). Contains
clustering results and LDA fit using all input variables. Columns
include \code{cluster_name}, \code{cluster_fit} (specc object), \code{cluster_seed}
(assignment tibble), \code{lda_fit}, \code{accuracy}, \code{df_append}, etc.
Note: no \code{cluster_glance} column (broom does not support specc).}
\item{reduced_inputs}{Same structure but LDA uses only the reduced
variable set selected by \code{\link[=cluster_reduce_vars]{cluster_reduce_vars()}}.}
}
}
\description{
Spectral clustering wrapper for the segmentation pipeline.
Runs \code{kernlab::specc()} across a range of cluster counts (k), reduces
inputs via discriminant analysis, and pipes results through
\code{\link[=cluster_add_lda]{cluster_add_lda()}} to produce a standardized output.

\strong{What spectral clustering does.} Spectral clustering embeds respondents
into a low-dimensional space derived from the eigenvectors of a similarity
(affinity) matrix, then clusters in that space. Specifically: (1) compute
a pairwise similarity matrix using an RBF (Gaussian) kernel, (2) form the
normalized graph Laplacian, (3) take the top k eigenvectors, (4) run
k-means on the eigenvector embedding. Because the embedding captures the
connectivity structure of the data rather than raw Euclidean distances,
spectral clustering can find clusters with non-convex shapes that k-means
and PAM miss entirely.

\strong{Strengths.} Can discover clusters of arbitrary shape — spirals, rings,
interleaving crescents — that distance-based methods (k-means, PAM) cannot
separate. The RBF kernel bandwidth (\code{sigma}) is estimated automatically by
\code{kernlab::specc()}. No assumption of spherical or equal-sized clusters.
Works well when the cluster structure is defined by density connectivity
rather than proximity to a center.

\strong{Limitations.} Requires computing and eigen-decomposing an n x n
similarity matrix — O(n^2) memory and O(n^3) time for the eigendecomposition.
This makes it significantly slower than k-means for large datasets. The
quality of results is sensitive to the kernel bandwidth parameter. The
final step is still k-means on the embedding, so results can vary across
runs (mitigated by setting \code{seed}). No built-in \code{broom::glance()} support,
so the return value has no \code{cluster_glance} column.

\strong{Pipeline integration.} This is a method wrapper called by
\code{\link[=cluster_solution_family]{cluster_solution_family()}} (and indirectly by \code{\link[=seg_cluster_input_sheet]{seg_cluster_input_sheet()}}).
All method wrappers share the same signature and return shape so they
can be dispatched interchangeably.
}

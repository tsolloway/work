% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_insights.R
\name{bn_insights}
\alias{bn_insights}
\title{bn_insights}
\usage{
bn_insights(
  x,
  verbose = TRUE,
  api_key = NULL,
  model = "claude-sonnet-4-5-20250929",
  max_tokens = 2000L,
  top_n = 10L,
  audience = c("client", "internal"),
  dry_run = FALSE
)
}
\arguments{
\item{x}{One of:
\itemize{
\item The return value of \code{bn_write()} — a \code{bn_write_result} carrying \verb{$obj},
\verb{$title}, \verb{$sub_title}, \verb{$dv_display}. Recommended.
\item A \code{bn_finalize_network()} output directly. Title/subtitle/DV fall back
to NULL and whatever is in \verb{$impacts$meta}.
\item A list of either of the above, one per result. In that case each is
analyzed independently and the narratives are concatenated.
}}

\item{verbose}{Logical. When TRUE (default) the narrative is printed to the
console via \code{cat()}. When FALSE the function is silent; the return value
still carries the narrative and the digest.}

\item{api_key}{Character or NULL. Anthropic API key. If NULL (default), it
is loaded via \code{get_environment_key("ANTHROPIC_API_KEY")} — reads from the
environment variable first, and falls back to an interactive
\code{rstudioapi::askForPassword()} prompt if unset.}

\item{model}{Character. Anthropic model id. Default
\code{"claude-sonnet-4-5-20250929"}.}

\item{max_tokens}{Integer. Upper bound on the generated response length.
Default 2000.}

\item{top_n}{Integer. How many top attribute drivers / top communities /
prioritization steps to include in the digest sent to the API. Default 10.}

\item{audience}{One of \code{"client"} (default — plain language, no methods
jargon) or \code{"internal"} (uses metric names directly). Controls the prompt
framing sent to the API.}

\item{dry_run}{Logical. When TRUE, the function builds the digest and
returns it WITHOUT calling the Anthropic API. Useful for inspecting what
the LLM would see (and verifying top-N ordering) without spending
tokens. Default FALSE.}
}
\value{
A list (invisibly) with elements:
\itemize{
\item \code{narrative} — the analyst-generated text (character scalar).
\item \code{digest} — the structured summary that was sent to the API (useful for
auditing / regression testing).
\item \code{model}, \code{usage} — the Anthropic response metadata.
}
}
\description{
Produces a client-facing narrative analysis of a Bayesian
network's results (impacts + prioritization) by extracting the key
findings from the R objects and sending a structured digest to Anthropic's
Claude API for plain-language interpretation. Designed to consume the
return value of \code{bn_write()} directly.
}
\details{
The API key is only read when the function is actually called, never at
package load time. The call uses \code{httr2::request()} with a default retry
policy on transient 5xx / rate-limit errors.
}

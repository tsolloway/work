% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_cluster_prototype_seed.R
\name{seg_cluster_prototype_seed}
\alias{seg_cluster_prototype_seed}
\title{seg_cluster_prototype_seed}
\usage{
seg_cluster_prototype_seed(
  seg,
  solution_family_name,
  seed_name,
  seed = NULL,
  vars = NULL,
  filter_name = NULL,
  use_greedy = TRUE,
  use_top_n_polars = 20,
  reduced_inputs_max = 14,
  profile_lda_ratio = NULL,
  force_inputs = NULL,
  force_remove_lda_inputs = NULL,
  priors = NULL,
  resp_id_name = NULL,
  keep_raw = FALSE
)
}
\arguments{
\item{seg}{A seg object with data and solutions loaded.}

\item{solution_family_name}{Character. The solution family key (e.g.
\code{"D7to5"}). Results are stored under
\code{seg$solutions$analysis[[solution_family_name]]}.}

\item{seed_name}{Character. Column name in the seed tibble (and in
\code{seg$data$with_solutions}) containing the segment assignments
(e.g. \code{"proto_D7to5"}).}

\item{seed}{Data frame or \code{NULL}. Two-column tibble with respondent IDs and
segment assignments. If \code{NULL}, extracted from \code{seg$data$with_solutions}
using \code{resp_id_name} and \code{seed_name}.}

\item{vars}{Character vector or \code{NULL}. Polar input variables for LDA. If
\code{NULL} and \code{use_greedy = TRUE}, selected automatically via
\code{\link[=get_greedy_vars]{get_greedy_vars()}}. If \code{NULL} and \code{use_greedy = FALSE}, uses all RS
polars from the spec.}

\item{filter_name}{Character or \code{NULL}. Column name containing a logical
filter (e.g. \code{"okay_filter"}). \code{NULL} uses all rows.}

\item{use_greedy}{Logical. If \code{TRUE} (default), select polar inputs via
greedy Wilks' Lambda when \code{vars = NULL}.}

\item{use_top_n_polars}{Integer. Number of top polars to consider during
greedy variable selection (default: \code{20}).}

\item{reduced_inputs_max}{Integer or character vector or \code{NULL}. Cap on the
number of reduced input variables. If numeric, truncates the greedy
selection to this many. If character, uses exactly those variables. \code{NULL}
uses all selected variables. Default: \code{14}.}

\item{profile_lda_ratio}{Numeric between 0 and 1, or \code{NULL}. Target proportion
of reduced LDA inputs that should come from profile (non-polar) variables.
Rounding favours the polar count. Default: \code{NULL} (no balancing — all vars
compete equally in the ranked list).}

\item{force_inputs}{Character vector or \code{NULL}. Variable names to always
include in the optimised (reduced) LDA input set, even if they are not
ranked in the top N by \code{cluster_reduce_vars}. Appended after
budget/cap selection. Default: \code{NULL}.}

\item{force_remove_lda_inputs}{Character vector or \code{NULL}. Variable names to
remove from the optimised (reduced) LDA input set after
\code{reduced_inputs_max}, \code{profile_lda_ratio}, and \code{force_inputs} are all
applied. Default: \code{NULL}.}

\item{priors}{Character or numeric vector. LDA prior method: \code{"size"} (default),
\code{"equal"}, or a numeric vector of length k (one value per segment, will be
normalised to sum to 1). If \code{NULL}, defaults to \code{"equal"}.}

\item{resp_id_name}{Character or \code{NULL}. Respondent ID column. If \code{NULL},
auto-detected via \code{\link[=get_resp_id_name]{get_resp_id_name()}}.}
}
\value{
The seg object with updated \code{seg$solutions$analysis},
\code{seg$solutions$summary_table}, \code{seg$solutions$df_segment_append}, and
\code{seg$data$with_solutions}.
}
\description{
Fits a segmentation solution from a pre-defined prototype seed.
Instead of discovering clusters from scratch (like \code{\link[=seg_cluster_input_sheet]{seg_cluster_input_sheet()}}
or \code{\link[=seg_cluster_with_profiles]{seg_cluster_with_profiles()}}), this function takes an existing set of
segment assignments — the "seed" — and builds the full LDA typing tool
around it.

Typical use case: you have a hand-crafted or modified prototype solution
(e.g., from \code{\link[=seg_prototype_split_segments]{seg_prototype_split_segments()}}) where segments were manually
merged, split, or reassigned. This function takes those assignments as
ground truth and produces the discriminant analysis, confusion matrix,
accuracy metrics, and segment append columns — everything needed to
evaluate and export the solution.

\strong{Workflow:}
\enumerate{
\item Extracts the seed assignments (from \code{seg$data$with_solutions} or a
provided tibble).
\item Optionally selects polar inputs via greedy Wilks' Lambda
(\code{\link[=get_greedy_vars]{get_greedy_vars()}}).
\item Reduces inputs via \code{\link[=cluster_reduce_vars]{cluster_reduce_vars()}} (greedy_step, falling back
to stepwise if greedy_step fails).
\item Fits LDA twice via \code{\link[=cluster_add_lda]{cluster_add_lda()}} — once with all inputs, once
with the reduced set.
\item Merges results into the solution family in \code{seg$solutions$analysis},
rebuilds the global summary table and segment append columns.
}
}

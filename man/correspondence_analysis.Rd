% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correspondence_analysis.R
\name{correspondence_analysis}
\alias{correspondence_analysis}
\title{correspondence_analysis}
\usage{
correspondence_analysis(
  stack_result,
  vars = NULL,
  weight = NULL,
  normalize = FALSE,
  supplemental_base = 75,
  exclude_base = 40,
  assigned_only = TRUE
)
}
\arguments{
\item{stack_result}{List returned by \code{\link[=stack_data]{stack_data()}}. Must contain \code{df_stack},
\code{dictionary_stack}, and \code{var_types}.}

\item{vars}{Character vector of stacked variable names to use as the
attributes (map columns). Default \code{NULL} uses every variable tagged as an
IV in \code{dictionary_stack}.}

\item{weight}{Character scalar or \code{NULL}. Name of a weight column in
\code{df_stack}. When \code{NULL} (default) means are unweighted; otherwise weighted
means are used. The base thresholds always use unweighted counts.}

\item{normalize}{Logical. If \code{TRUE}, each attribute column of the means
matrix is divided by its column total before the analysis, so every
attribute carries equal mass regardless of its scale. Use when mixing
attributes measured on different scales (e.g. 0-10 means alongside 0/1
incidences). Default \code{FALSE}.}

\item{supplemental_base}{Numeric. Brands or attributes whose base is below
this value are projected as supplementary points. Default \code{75}.}

\item{exclude_base}{Numeric. Brands or attributes whose base is below this
value are excluded before the analysis. Default \code{40}.}

\item{assigned_only}{Logical. If \code{TRUE} (default) and the stack has an
assigner variable, restrict to assigned rows before computing means.}
}
\value{
A named list:
\item{coordinates}{Tibble of principal coordinates (\code{dim_1}, \code{dim_2}, ...)
with \code{point}, \code{type} (\code{"brand"}/\code{"attribute"}), \code{base}, and
\code{supplementary}.}
\item{inertia}{Tibble with \code{dimension}, \code{singular_value}, \code{inertia_pct},
and \code{inertia_cumulative}.}
\item{matrix}{The brand-by-attribute matrix that was analysed (normalized
when \code{normalize = TRUE}).}
\item{bases}{Tibble of \code{point}, \code{type}, \code{base}, and \code{status}
(\code{"active"}/\code{"supplementary"}/\code{"excluded"}).}
\item{ca}{The raw \code{\link[ca:ca]{ca::ca()}} object.}
\item{meta}{List of the call settings (\code{weight}, \code{supplemental_base},
\code{exclude_base}, \code{assigned_only}).}
}
\description{
Run correspondence analysis on a brand-by-attribute matrix of
means derived from a \code{\link[=stack_data]{stack_data()}} result. Means are computed with
\code{\link[=means_summary]{means_summary()}} (weighted when \code{weight} is supplied). Brands or attributes
with thin bases are handled two ways: those below \code{supplemental_base} are
projected as \emph{supplementary} points (drawn on the map but excluded from the
construction of the axes), and those below \code{exclude_base} are dropped
entirely before the analysis. Bases are always unweighted respondent counts.
}

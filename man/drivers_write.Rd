% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drivers_write.R
\name{drivers_write}
\alias{drivers_write}
\title{drivers_write}
\usage{
drivers_write(
  drivers_result,
  file_name = NULL,
  sub_title = NULL,
  label_width = "auto",
  single_workbook = FALSE,
  path = "."
)
}
\arguments{
\item{drivers_result}{The output of \code{drivers()}.}

\item{file_name}{Character. Prefix for output file names. Files are saved as
\code{{file_name} - Drivers of {dv}.xlsx}. If NULL, inherits from
\code{sub_title}. If both are NULL, files are saved as
\code{Drivers of {dv}.xlsx}.}

\item{sub_title}{Character. Subtitle text (e.g. project name). Default NULL.}

\item{label_width}{Column width for the Label column (default \code{"auto"}).}

\item{single_workbook}{Logical. If TRUE, all sheets are written to a single
workbook. Sheet names are prefixed with the DV name (e.g.
\code{{dv} - {ivs}}). Default FALSE.}

\item{path}{Directory to write workbooks to (default \code{"."}).}
}
\value{
Named list of workbook objects (invisibly).
}
\description{
Takes the output of \code{drivers()} and writes formatted Excel
workbooks. One workbook per DV, one sheet per IV set. Reads metadata
(engine, shift_percentage, subgroups) from the result object.
}

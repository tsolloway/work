% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_visual.R
\name{bn_visual}
\alias{bn_visual}
\title{Visualize Bayesian Networks with Multiple Layout Modes}
\usage{
bn_visual(
  obj,
  type = c("none", "gravity", "charge", "hierarchy"),
  nodes = NULL,
  edges = NULL,
  vs_height = "100vh",
  vs_width = "100\%",
  gravity_constant = -9000,
  central_gravity = 0.2,
  charge_layout = "layout_with_fr",
  do_community = FALSE,
  add_key = TRUE,
  key_width = 0.1,
  node_positions = NULL,
  interactive = TRUE,
  physics = TRUE,
  panel_ns = NULL,
  download_prefix = "network",
  save_visuals = FALSE,
  save_to_png = FALSE,
  save_file_name = "Network Visual",
  png_width = 1200,
  png_height = 900,
  png_delay = 3,
  font_size = NULL,
  seed = 1
)
}
\arguments{
\item{obj}{A Bayesian network visualization object or list containing
at least \code{nodes} and \code{edges} data frames. Can also be a higher-level
object from \code{bn_engine()} or similar that includes \code{viz_prep}.}

\item{type}{Character. Layout type: one of
\code{"none"}, \code{"gravity"}, \code{"charge"}, or \code{"hierarchy"}. Default \code{"none"}.}

\item{nodes}{Optional data frame of nodes to override those in \code{obj}.
Must include columns: \code{id}, \code{label}, and optionally \code{group}, \code{color}, etc.}

\item{edges}{Optional data frame of edges to override those in \code{obj}.
Must include columns: \code{from}, \code{to}.}

\item{vs_height}{Character. Height of the visualization container
(e.g., \code{"100vh"}). Default \code{"100vh"}.}

\item{vs_width}{Character. Width of the visualization container
(e.g., \code{"100\%"}). Default \code{"100\%"}.}

\item{gravity_constant}{Numeric. Gravitational constant controlling
repulsion strength in Barnes–Hut layout (more negative = stronger repulsion).
Default \code{-9000}.}

\item{central_gravity}{Numeric. Pull of all nodes toward the center.
Default \code{0.2}.}

\item{charge_layout}{Character. Name of an \pkg{igraph} layout function to use
when \code{type = "charge"}. Controls how nodes are spatially arranged using
force-directed, geometric, or hierarchical algorithms.

Any valid layout name recognized by \pkg{igraph} may be used (passed
to \code{igraph::layout_*(graph)} internally). Common and extended options include:

\strong{Force-directed and physics-based layouts}
\itemize{
\item \code{"layout_with_fr"} — Fruchterman–Reingold (default); balances attractive
and repulsive forces for a visually even distribution.
\item \code{"layout_with_kk"} — Kamada–Kawai spring layout; ideal for symmetric networks.
\item \code{"layout_with_drl"} — Distributed Recursive Layout; scalable variant of FR
suitable for large graphs.
\item \code{"layout_with_gem"} — GEM (Graph Embedder) layout; force-directed with adaptive cooling.
}

\strong{Deterministic geometric layouts}
\itemize{
\item \code{"layout_in_circle"} — Places all nodes evenly spaced on a circle.
\item \code{"layout_in_grid"} — Arranges nodes on a rectangular grid.
\item \code{"layout_randomly"} — Uniform random positions within a bounding box.
\item \code{"layout_norm"} — Normalizes coordinates of any layout to the unit square.
}

\strong{Hierarchical and structural layouts}
\itemize{
\item \code{"layout_as_tree"} — Rooted tree layout; useful for DAGs or BN structures.
\item \code{"layout_as_star"} — Star layout centered on a single focal node.
\item \code{"layout_as_bipartite"} — Bipartite graph layout with two distinct node sets.
}

\strong{Dimensional-reduction layouts}
\itemize{
\item \code{"layout_with_mds"} — Multidimensional scaling (MDS) layout; positions nodes
based on pairwise graph distances.
\item \code{"layout_with_lgl"} — Large Graph Layout; optimized for very large networks.
\item \code{"layout_with_stress"} — Stress majorization layout (newer, flexible alternative
to Kamada–Kawai).
}}

\item{do_community}{Logical. If \code{TRUE}, visualize community-level network
instead of node-level attributes (requires community prep).}

\item{add_key}{Logical. If \code{TRUE}, adds a legend for community or group colors.
Default \code{TRUE}.}

\item{key_width}{Numeric. Width of the legend as a proportion of the widget
(0 to 1). Default \code{0.1} (10\%).}

\item{node_positions}{Optional. Pre-saved node positions to restore a
previous layout. Accepts:
\itemize{
\item A file path to a JSON file exported by the "Save Layout" button.
\item A data frame with columns \code{id}, \code{x}, \code{y}.
When provided, node coordinates are set before rendering and physics
is disabled to preserve the layout.
}}

\item{interactive}{Logical. If \code{TRUE}, adds enhanced interactivity (zoom,
drag, font slider, PNG/SVG export) via
\code{work::bn_visNetwork_deliverable_interactivity()}.}

\item{save_visuals}{Logical. If \code{TRUE}, saves the visualization as a
standalone HTML file.}

\item{save_to_png}{Logical. If \code{TRUE}, captures the visualization as a
PNG image using \code{webshot2}. Requires the \code{webshot2} package.
Default \code{FALSE}.}

\item{save_file_name}{Character. File name (without extension) used for
saved HTML or PNG files. Default \code{"Network Visual"}.}

\item{png_width}{Numeric. PNG capture width in pixels. Default 1200.}

\item{png_height}{Numeric. PNG capture height in pixels. Default 900.}

\item{png_delay}{Numeric. Seconds to wait for physics stabilization before
capturing PNG. Default 3. Bump if you see unsettled/unstable layouts in
the rendered image on larger networks.}

\item{font_size}{Numeric or NULL. Override the node-label font size (in
pixels) by applying \code{visNetwork::visNodes(font = list(size = ...))}.
Default NULL keeps vis.js's built-in 14 px. In interactive mode the
in-iframe font slider can still adjust this afterwards.}

\item{seed}{Numeric. Random seed for reproducible layout placement.}
}
\value{
A \code{visNetwork} object representing the network visualization.
When \code{save_visuals = TRUE}, an \code{.html} file is also written to disk.
}
\description{
Creates an interactive or static visualization of a Bayesian Network
using \pkg{visNetwork}. Supports multiple layout paradigms:
\itemize{
\item \code{"none"}: fixed coordinates or static layout without physics.
\item \code{"charge"}: Fruchterman–Reingold charge-based layout (via \code{igraph}).
\item \code{"gravity"}: Barnes–Hut gravity-based physics simulation (dynamic).
\item \code{"hierarchy"}: directed or layered layout (hierarchical).
}

Designed for deliverable-quality visuals or exploratory interactivity.
Integrates seamlessly with Bayesian network outputs from \code{bnlearn}
and visualization prep outputs from custom BN pipelines.
}
\details{
The function supports four conceptual layout paradigms:\tabular{llll}{
   Type \tab Layout Engine \tab Physics \tab Conceptual Model \cr
   \code{"none"} \tab None \tab N \tab Static snapshot (fixed positions) \cr
   \code{"charge"} \tab Fruchterman–Reingold \tab N \tab Charge repulsion (balanced electrostatic field) \cr
   \code{"gravity"} \tab Barnes–Hut \tab Y. \tab Gravity simulation (mass clustering) \cr
   \code{"hierarchy"} \tab vis.js hierarchical \tab N \tab Layered top-down or directional layout \cr
}
}
\examples{
\dontrun{
library(bnlearn)
library(visNetwork)

# Example 1: Basic static visualization (no physics)
bn <- bnlearn::hc(iris)
net <- bnlearn::graphviz.plot(bn) # for reference

nodes <- data.frame(id = nodes(bn), label = nodes(bn))
edges <- bnlearn::arcs(bn) \%>\% as.data.frame()

bn_visual(
  obj = list(nodes = nodes, edges = edges),
  type = "none"
)

# Example 2: Charge-based layout (Fruchterman–Reingold)
bn_visual(
  obj = list(nodes = nodes, edges = edges),
  type = "charge"
)

# Example 3: Gravity layout (Barnes–Hut physics)
bn_visual(
  obj = list(nodes = nodes, edges = edges),
  type = "gravity",
  gravity_constant = -7000
)

# Example 4: Hierarchical layout
bn_visual(
  obj = list(nodes = nodes, edges = edges),
  type = "hierarchy"
)
}

}

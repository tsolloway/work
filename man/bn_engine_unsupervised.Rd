% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_engine_unsupervised.R
\name{bn_engine_unsupervised}
\alias{bn_engine_unsupervised}
\title{Engine to Build and Summarize an Unsupervised Bayesian Network}
\usage{
bn_engine_unsupervised(
  df,
  ivs,
  dictionary = NULL,
  manual_groups = NULL,
  white_list = NULL,
  black_list = NULL,
  algorithm = c("tabu", "hc"),
  score = c("bic", "aic"),
  connections_max = 1,
  connections_multiple_boot_threshold = "auto",
  connections_multiple_boot_ratio = 1/4,
  cross_battery_priority = TRUE,
  only_white_list = FALSE,
  node_label_type = c("both", "variable", "label"),
  n_groups = NULL,
  node_size = 1,
  ensure_connectivity = TRUE,
  connectivity_boot_n = 50,
  complexity_boot_n = 100,
  complexity_boot_strength_min = 0.01,
  suppress_bn_warning = FALSE,
  on_exit_detach_igraph = TRUE,
  tool_tip_edge_prefix = NULL,
  seed = 1
)
}
\arguments{
\item{df}{Data frame containing the variables to model.}

\item{ivs}{Character vector or list of character vectors. Variable names to include.
When a list of batteries, \code{cross_battery_priority} controls within-battery arcs.}

\item{dictionary}{Optional mapping or data frame for pretty labels.}

\item{manual_groups}{Optional data frame of node-level group/color/labels for viz.}

\item{white_list}{Optional data frame with columns \code{from}, \code{to} of arcs to force include.}

\item{black_list}{Optional data frame with columns \code{from}, \code{to} of arcs to blacklist.}

\item{algorithm}{Character. Structure learning algorithm: \code{"tabu"} or \code{"hc"}.}

\item{score}{Character. Scoring function: \code{"bic"} or \code{"aic"}.}

\item{connections_max}{Integer (>= 1). Maximum number of parents per node.
1 = base structure only; higher values add layers via \code{bn_increase_complexity()}.}

\item{connections_multiple_boot_threshold}{Numeric in [0,1] or \code{"auto"}.
Threshold for arc strength when adding layers.}

\item{connections_multiple_boot_ratio}{Numeric in (0,1]. When \code{threshold = "auto"},
proportion of top arcs to retain.}

\item{cross_battery_priority}{Logical. If \code{TRUE} and \code{ivs} is a list of batteries,
blacklists within-battery arcs so the base structure favors cross-battery connections.}

\item{only_white_list}{Logical. If \code{TRUE}, skips structure learning and uses only \code{white_list}.}

\item{node_label_type}{One of \code{c("both","variable","label")}. Label style in viz.}

\item{n_groups}{Optional integer. Target number of communities for viz.}

\item{node_size}{Numeric. Node size scaling for viz.}

\item{ensure_connectivity}{Logical. If \code{TRUE} (default), checks that the learned
network is fully connected and bridges disconnected components using bootstrapped
arc strengths via \code{bn_ensure_connectivity()}.}

\item{connectivity_boot_n}{Integer. Number of bootstrap replicates for connectivity bridging.}

\item{complexity_boot_n}{Integer (default = 100). Number of bootstrap replicates
for complexity expansion via \code{bn_increase_complexity()}.}

\item{complexity_boot_strength_min}{Numeric (default = 0.01). Minimum arc strength
retained during complexity expansion.}

\item{suppress_bn_warning}{Logical. Suppress BIC direction warning.}

\item{on_exit_detach_igraph}{Logical. Detach igraph on exit.}

\item{tool_tip_edge_prefix}{Optional character string to prepend to edge tooltips.}

\item{seed}{Optional integer. Set seed for reproducible structure learning.}
}
\value{
A list with elements:
\itemize{
\item \code{bn}: the learned \code{bnlearn} network.
\item \code{fit}: the fitted parameters (\code{bn.fit}).
\item \code{viz_prep}: visualization-prep object from \code{bn_to_netviz_prep()}.
\item \code{summary}: model summary list from \code{bn_summary_statistics()} (no accuracy metrics).
\item \code{results_layers} (optional): list with per-layer results when \code{connections_max > 1}.
\item \code{meta}: list with metadata (\code{analysis = "bn_model_unsupervised"}).
}
}
\description{
Builds a Bayesian network without a dependent variable using score-based
structure learning (tabu or hill climbing). Optionally enforces cross-battery
priority, ensures full connectivity, and layers additional parent connections
via bootstrapped arc strengths. Returns the network, fitted model,
visualization prep, and model summary.
}
\details{
\strong{Workflow}
\enumerate{
\item Learn base structure via \code{bnlearn::tabu()} or \code{bnlearn::hc()} with \code{maxp = 1}.
When \code{cross_battery_priority = TRUE} and \code{ivs} is a list, within-battery arcs
are blacklisted so the base structure only learns cross-battery connections.
\item Optionally ensure full connectivity via \code{bn_ensure_connectivity()}.
\item Fit with \code{bnlearn::bn.fit(method = "bayes")}.
\item Optionally add layers of additional parents up to \code{connections_max} using
\code{bn_increase_complexity()}.
\item Prepare network visualization with \code{bn_to_netviz_prep()}.
}

This is the unsupervised counterpart to \code{\link[=bn_engine]{bn_engine()}}, which requires a DV
and uses a TAN backbone. The return structure is identical except \code{meta$analysis}
is \code{"bn_model_unsupervised"} and there is no \code{dv} in metadata.
}
\examples{
\dontrun{
library(bnlearn)
library(dplyr)
df <- iris \%>\% mutate(across(everything(), as.factor))
ivs <- names(df)

out <- bn_engine_unsupervised(
  df = df,
  ivs = ivs,
  connections_max = 2,
  seed = 1
)

out[["bn"]]
out[["summary"]][["model"]]
}

}

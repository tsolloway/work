% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_short_form_analysis.R
\name{seg_short_form_analysis}
\alias{seg_short_form_analysis}
\title{seg_short_form_analysis}
\usage{
seg_short_form_analysis(
  seg,
  solution,
  reorder = NULL,
  solution_new = NULL,
  id_name = NULL,
  priors = c("equal", "size"),
  reduce_type = c("greedy_step", "greedy", "step"),
  remove_first = NULL,
  min_items = NULL,
  seed = 1
)
}
\arguments{
\item{seg}{A seg object with solutions in \code{seg$solutions$summary_table}.}

\item{solution}{Character. Name of the solution to analyze. Matched first
against \code{solution_name}, then against \code{lda_name}.}

\item{reorder}{Optional numeric vector to reorder segments before analysis.
Maps old segment numbers to new (e.g., \code{c(6,2,3,4,5,1)} means old
segment 1 becomes new segment 6, old 6 becomes new 1). If provided, calls
\code{\link[=seg_reorder_solution]{seg_reorder_solution()}} first.}

\item{solution_new}{Character. Name for the reordered solution. Required if
\code{reorder} is not NULL. Defaults to \code{NULL}.}

\item{id_name}{Character. Name of the respondent ID column. Defaults to
\code{seg$meta$id_variable}.}

\item{priors}{Character. Prior probabilities for LDA. One of \code{"equal"} or
\code{"size"}. Defaults to \code{"equal"}.}

\item{reduce_type}{Character. Variable reduction strategy passed to
\code{\link[=cluster_reduce_vars]{cluster_reduce_vars()}}. One of \code{"greedy_step"}, \code{"greedy"}, or \code{"step"}.
Defaults to \code{"greedy_step"}.}

\item{remove_first}{Character vector. Variables to remove first (before
the greedy ranking kicks in). Listed in removal order — first element
is removed first. Defaults to \code{NULL}.}

\item{min_items}{Integer. Minimum number of items to evaluate. Stops the
analysis at this item count or at failure, whichever comes first.
Defaults to \code{NULL} (go as low as possible).}

\item{seed}{Numeric. Random seed for reproducibility. Defaults to \code{1}.}
}
\value{
A list with two elements:
\describe{
\item{results}{Full results tibble with columns: \code{n} (item count),
\code{inputs}, \code{lda_inputs}, \code{lda_fit}, \code{lda_coefficient_function},
\code{lda_seg}, \code{confusion}, \code{accuracy_overall}, \code{accuracy_seg}.}
\item{analysis}{Summary tibble with columns: \code{Items},
\code{Overall_Accuracy}, and per-segment \verb{Precision_Seg_*} / \verb{Recall_Seg_*}.}
}
}
\description{
Performs short-form typing tool analysis on a segmentation
solution. Iteratively reduces the number of LDA input variables from the
full set down to the number of segments, re-fitting the LDA at each step.
Returns accuracy metrics (overall + per-segment precision/recall) at each
item count so you can identify the minimum number of items needed for an
acceptable typing tool.

Uses \code{\link[=cluster_reduce_vars]{cluster_reduce_vars()}} with greedy stepwise selection to rank
variables by discriminant importance, then refits LDA at each item count
from full down to k (number of segments).
}

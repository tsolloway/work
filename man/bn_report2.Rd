% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_report2.R
\name{bn_report2}
\alias{bn_report2}
\title{bn_report2}
\usage{
bn_report2(
  results,
  types = c("none", "gravity", "charge", "hierarchy"),
  do_community = c(TRUE, FALSE),
  title = "Network Analysis",
  subtitle = "Project Name (123456789)",
  interactive = TRUE,
  physics = FALSE,
  default_type = "gravity",
  gravity_constant = -9000,
  central_gravity = 0.2,
  charge_layout = "layout_with_fr",
  add_key = TRUE,
  self_contained = TRUE,
  save_name = NULL,
  file = NULL,
  open = TRUE,
  seed = 1,
  add_additional_results = FALSE,
  results_excel = NULL,
  qc_mode = FALSE,
  impact_outcome_display = NULL,
  shift_type = c("absolute", "proportional", "headroom", "range"),
  add_prioritization_pvalue = FALSE,
  prioritize_display = NULL
)
}
\arguments{
\item{results}{A named list of engine results
(e.g., \code{list(cb = bn_init$cb, ncb = bn_init$ncb)}),
or a single engine result (auto-wrapped).
Also accepts the full output of \code{bn_initial_networks()} directly.}

\item{types}{Character vector of layout types to render.
Default \code{c("none", "gravity", "charge")}.}

\item{do_community}{Logical vector controlling which views to render.
Default \code{c(FALSE, TRUE)} renders both attribute and community views.
Use \code{FALSE} for attribute-only or \code{TRUE} for community-only.}

\item{title}{Character. Report title displayed as H1 header.
Default \code{"Network Analysis"}.}

\item{subtitle}{Character or \code{NULL}. Optional subtitle displayed below the
title, above the border line. Default \code{NULL} (no subtitle).}

\item{interactive}{Logical. Passed through to \code{bn_visual()}.
Default \code{TRUE}.}

\item{physics}{Logical. If \code{TRUE} (default), networks render with physics
enabled (nodes repel/attract in real time). If \code{FALSE}, networks render
with physics to compute layout, then physics is disabled after
stabilization so nodes stay fixed.}

\item{default_type}{Character or \code{NULL}. Which layout type to show by
default in the dropdown selector. Must be one of \code{types}. If \code{NULL}
(default), uses the first element of \code{types}.}

\item{gravity_constant}{Numeric. Gravitational constant for the gravity
layout. Passed through to \code{bn_visual()}. Default \code{-9000}.}

\item{central_gravity}{Numeric. Central gravity strength for the gravity
layout. Passed through to \code{bn_visual()}. Default \code{0.2}.}

\item{charge_layout}{Character. igraph layout algorithm for the charge
layout. Passed through to \code{bn_visual()}. Default \code{"layout_with_fr"}.}

\item{add_key}{Logical. If \code{TRUE}, adds a community color legend to each
network. Default \code{FALSE} (legend can cause overflow in iframes).}

\item{self_contained}{Logical. If \code{TRUE} (default), embeds all widget
JS/CSS into a single HTML file via base64 iframes. If \code{FALSE}, writes
widget dependencies to a \verb{lib/} folder alongside the HTML file (faster
to generate, but not portable as a single file).}

\item{save_name}{Character or \code{NULL}. Name portion of the Save Layout
filename (without extension). If \code{NULL} (default), auto-generates from
\code{title} and \code{subtitle}.}

\item{file}{Character or \code{NULL}. Output HTML file path. If \code{NULL} (default),
auto-generates from \code{title} and \code{subtitle} (e.g., \code{"Network Analysis.html"}
or \code{"Network Analysis - Feb 2026.html"}).}

\item{open}{Logical. If \code{TRUE}, opens the file in the browser.
Default \code{FALSE}.}

\item{seed}{Numeric. Passed through to \code{bn_visual()}. Default \code{1}.}

\item{add_additional_results}{Logical. If \code{TRUE}, and a passed result
(e.g., the output of \code{bn_finalize_network()}) contains \verb{$impacts}
and/or \verb{$prioritizations}, those tables are rendered as extra tabs
in each accordion section: "Attribute Impacts", "Community Impacts"
(when community results are present), and "Prioritization". Styling
matches the \code{bn_write()} dashboards (grey header fill, color-coded
p-values). The Prioritization tab reads its \code{sig_threshold} and
\code{marginal_threshold} from \code{prioritizations$meta}, so the colour bands
stay consistent with whatever was set at \code{bn_finalize_network()} /
\code{bn_prioritizations()} time. Default \code{FALSE}.}

\item{results_excel}{Optional. Path(s) to prebaked \code{.xlsx} file(s) (e.g.
produced by \code{bn_write()}) to embed as \strong{Download Report} buttons on
each accordion. Accepted shapes:
\itemize{
\item \code{NULL} (default) — no Download Report buttons.
\item A single string — used for the (single) result.
\item A named list/vector — matched to \code{results} by name; missing or NULL
slots get no button.
\item An unnamed list/vector — matched to \code{results} by position; trailing
unmatched slots get no button.
}}

\item{qc_mode}{Logical. Enables QC-mode affordances in the HTML report
— currently: hover tooltips on Impact dashboard cells that expose the
underlying raw metric value and unrounded index (useful for validating
that computed values line up with expectations, spotting outliers,
cross-referencing Excel). Intentionally off for client-facing reports
so the displayed integer indices aren't shadowed by scientific-notation
hover text. Default \code{FALSE}.}

\item{impact_outcome_display}{Character or \code{NULL}. Initial value of the
impact dashboard's Outcome dropdown — \code{"Point Change"} or \code{"\% Change"}.
\code{NULL} (default) auto-detects from the DV type (dichotomous outcomes
default to \code{"Point Change"}, continuous outcomes default to
\code{"\% Change"}). Pass an explicit string to override.}

\item{add_prioritization_pvalue}{Logical. If \code{TRUE}, the prioritization
dashboard table includes its \code{p-value} column (current behavior). If
\code{FALSE} (default), the p-value column is removed from the prioritization
table.}

\item{prioritize_display}{Character or \code{NULL}. Initial value of the
prioritization Display dropdown — \code{"Point Change"} or \code{"\% Change"}.
\code{NULL} (default) auto-detects from the DV type (dichotomous outcomes
default to \code{"Point Change"}, continuous outcomes default to
\code{"\% Change"}). Pass an explicit string to override the auto-detection.}
}
\value{
The file path (invisibly).
}
\description{
Generates a self-contained HTML report containing multiple Bayesian Network
visualizations across layout types (none, gravity, charge) and views
(attribute-level, community-level). Accepts one or more engine results
from \code{bn_engine()}, \code{bn_engine_unsupervised()}, or \code{bn_initial_networks()}.

Results are shown in collapsible accordion sections. Within each section,
a dropdown selects the layout type and tabs switch between attribute and
community views.
}
\examples{
\dontrun{
# Full report from bn_initial_networks output
bn_init <- work::bn_initial_networks(df = df_bn, dv = NULL, batteries)
work::bn_report2(bn_init, file = "bn_exploratory.html")

# Single result, attribute-only, charge layout only
work::bn_report2(bn_init$cb, types = "charge", do_community = FALSE)

# Named comparison
work::bn_report2(
  list(Experimental = bn_init_exp$cb, Control = bn_init_ctrl$cb),
  title = "Kadro Millennials - BN Comparison"
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_rename_community_after_finalization.R
\name{bn_rename_community_after_finalization}
\alias{bn_rename_community_after_finalization}
\title{Rename communities in a finalized network object}
\usage{
bn_rename_community_after_finalization(obj, rename, verbose = TRUE)
}
\arguments{
\item{obj}{A list returned by \code{bn_finalize_network()}.}

\item{rename}{Named character vector of \code{c("old name" = "new name")}
pairs. Every old name must exist in the object; a new name that collides
with a community not itself being renamed is rejected (that would merge
two communities' labels while leaving their rows separate).}

\item{verbose}{Logical. Report which slots were updated. Default TRUE.}
}
\value{
\code{obj} with the community names replaced.
}
\description{
Renames one or more communities everywhere the name is stored
in a \code{bn_finalize_network()} result, so downstream writers
(\code{bn_write()}, \code{bn_report()}) stay consistent. Community names
live in several places - the attribute node table, the community network's
nodes and edges, and the impact tables - and editing only one of them
desyncs the workbook (e.g. a renamed Community Drivers sheet against an
old community map image).

Renaming after finalizing is safe because the community name is a label
only: membership, impacts, and the maps are keyed off the attribute
assignments, which are untouched. To change membership instead, use
\code{bn_move_attribute_to()} and re-finalize.
}
\examples{
\dontrun{
bn_final <- bn_final \%>\%
  bn_rename_community_after_finalization(c("Modern Expertise" = "Premium Signals"))
}

}
\seealso{
\code{\link[=bn_finalize_network]{bn_finalize_network()}}, \code{\link[=bn_move_attribute_to]{bn_move_attribute_to()}}
}

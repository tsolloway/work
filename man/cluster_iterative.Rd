% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_iterative.R
\name{cluster_iterative}
\alias{cluster_iterative}
\title{cluster_iterative}
\usage{
cluster_iterative(
  df,
  vars,
  vars_profiles,
  solution_name,
  solution_name_prefix = "iter",
  resp_id_name = "seg_uuid",
  filter_name = NULL,
  n_min = 4,
  n_max = 7,
  reduced_inputs_max = NULL,
  priors = c("equal", "size"),
  iter_max = 1000,
  nstart = 10,
  lda_vars = NULL,
  lda_vars_profiles = NULL,
  seed = 1,
  vars_shell,
  group_source_vars,
  group_polar_pvs,
  target = "total",
  target_defs = NULL,
  strategy = NULL,
  strategy_defs = NULL,
  polar_threshold = 0.2,
  profile_threshold = 0.15,
  min_seg_pct = 0.05,
  swap_max_iter = 1000,
  weight_var = NULL
)
}
\arguments{
\item{df}{A data frame containing shell data.}

\item{vars}{Character vector of source variable names to cluster on.}

\item{vars_profiles}{Character vector of polar profile variable names
(binary indicators derived from source vars).}

\item{solution_name}{Character. Solution identifier (e.g. \code{"A"}).}

\item{solution_name_prefix}{Character. Prefix for cluster names
(default: \code{"iter"}).}

\item{resp_id_name}{Character. Respondent ID column (default: \code{"seg_uuid"}).}

\item{filter_name}{Character. Column name for logical filter (e.g.
\code{"okay_filter"}). \code{NULL} uses all rows.}

\item{n_min}{Integer. Minimum clusters (default: \code{4}).}

\item{n_max}{Integer. Maximum clusters (default: \code{7}).}

\item{reduced_inputs_max}{Integer. Max reduced input variables for
discriminant analysis. \code{NULL} uses all.}

\item{priors}{Character. Prior probability method for LDA: \code{"equal"} or
\code{"size"} (default: \code{"equal"}).}

\item{iter_max}{Integer. Max k-means iterations for seeding
(default: \code{1000}).}

\item{nstart}{Integer. Number of random starts for k-means seeding
(default: \code{10}).}

\item{lda_vars}{Character vector. Override LDA input variables. \code{NULL}
uses \code{vars}.}

\item{lda_vars_profiles}{Character vector. Override LDA profile variables.
\code{NULL} uses \code{vars_profiles}.}

\item{seed}{Integer. Random seed (default: \code{1}).}

\item{vars_shell}{Character vector. Shell profile variable names used in
the evaluation matrix (e.g. from \code{seg_get_vars(seg, type = "profiles")}).}

\item{group_source_vars}{Named list. Each element is a character vector of
source variable names for one polar group
(e.g. \code{list(kg = kg_source, pr_tt = c(pr_source, tt_source))}).}

\item{group_polar_pvs}{Named list. Each element is a character vector of
polar profile variable names for one group
(e.g. \code{list(kg = kg_pv, pr_tt = c(pr_pv, tt_pv))}).}

\item{target}{Character. Which entry in \code{target_defs} to use as the
optimization objective (default: \code{"total"}). This controls which polar
batteries the optimizer tries to differentiate. Profile variables always
count regardless.

For example, with three polar groups (KG, PR, TT) and:

\if{html}{\out{<div class="sourceCode">}}\preformatted{target_defs = list(
  total      = c("kg", "pr_tt"),   # count hits from all polars
  kg_only    = "kg",               # count only KG polar hits
  pr_tt_only = "pr_tt"             # count only PR/TT polar hits
)
}\if{html}{\out{</div>}}

Setting \code{target = "total"} tells the optimizer to maximize
differentiation across all batteries. Setting \code{target = "kg_only"}
makes the optimizer ignore PR/TT polar separation entirely — it will
only move respondents when doing so improves KG differentiation (plus
profile differentiation, which always counts). This is useful when one
battery is the primary segmentation driver and others are secondary.

Note: \code{target} controls what the optimizer \emph{counts}. \code{strategy}
(separately) controls which source vars \emph{seed the initial k-means}.
You can seed with KG vars but optimize on all polars, or vice versa.}

\item{target_defs}{Named list. Each element is a character vector of
group names (keys from \code{group_source_vars}) whose polar hits count
toward that target. The \code{target} parameter selects which entry to use.
\code{NULL} defaults to \code{list(total = names(group_source_vars))} — a single
target that counts all groups.}

\item{strategy}{Character. Which strategy definition to use for k-means
seeding. Must be a key in \code{strategy_defs}. \code{NULL} uses the first strategy.}

\item{strategy_defs}{Named list. Each element is a character vector of group
names whose source vars seed k-means. \code{NULL} defaults to
\code{list(all = names(group_source_vars))}.}

\item{polar_threshold}{Numeric. Polar hit threshold (default: \code{0.20}).}

\item{profile_threshold}{Numeric. Profile hit threshold (default: \code{0.15}).}

\item{min_seg_pct}{Numeric (0-1). Minimum segment size as fraction
(default: \code{0.05}).}

\item{swap_max_iter}{Integer. Maximum optimizer iterations (default: \code{1000}).}

\item{weight_var}{Character. Weight variable column name. \code{NULL} for
unweighted.}
}
\value{
A list with \code{all_inputs} and \code{reduced_inputs} tibbles (same shape
as \code{\link[=cluster_kmeans]{cluster_kmeans()}}).
}
\description{
Runs iterative greedy swap optimization for a single solution
family across a range of cluster counts. Seeds each k with k-means on
strategy variables, then optimizes threshold hits via \code{\link[=iterative_optimize]{iterative_optimize()}}.
Pipes results through \code{\link[=cluster_add_lda]{cluster_add_lda()}} for discriminant analysis.

Matches the \code{\link[=cluster_kmeans]{cluster_kmeans()}} interface and return shape so it integrates
directly with \code{\link[=seg_cluster_input_sheet]{seg_cluster_input_sheet()}} and \code{\link[=cluster_solution_family]{cluster_solution_family()}}.
}

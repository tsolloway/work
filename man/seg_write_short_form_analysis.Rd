% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_write_short_form_analysis.R
\name{seg_write_short_form_analysis}
\alias{seg_write_short_form_analysis}
\title{seg_write_short_form_analysis}
\usage{
seg_write_short_form_analysis(
  short_form_analysis,
  where = getwd(),
  file_name = NULL,
  project_name = NULL,
  segment_labels = NULL,
  segment_descriptions = NULL
)
}
\arguments{
\item{short_form_analysis}{Output list from \code{\link[=seg_short_form_analysis]{seg_short_form_analysis()}}. Must contain
\verb{$analysis} and \verb{$results}. Project metadata (\code{project_name}, \code{project_number})
is pulled from this object when \code{project_name} is \code{NULL}.}

\item{where}{Character. Directory to save the Excel file. Defaults to
the current working directory.}

\item{file_name}{Character. File name (without extension). If \code{NULL},
defaults to \code{"{project_name} - Short Form Analysis"}.}

\item{project_name}{Character. Project name displayed in the subtitle row.
If \code{NULL}, derived from \code{short_form_analysis$project_name} and/or
\code{short_form_analysis$project_number}. Falls back to \code{"Project (123456789)"}.}

\item{segment_labels}{Character vector. Short labels for each segment in
order (e.g., \code{c("IRS", "BCC", "LPLB", "AE", "W&S", "TPP")}). If \code{NULL},
defaults to \code{"Seg 1"}, \code{"Seg 2"}, etc.}

\item{segment_descriptions}{Named character vector. Full descriptions keyed
by segment label (e.g., \code{c("IRS" = "Involved Rx Skeptics", ...)}). If
\code{NULL}, the legend section is omitted.}
}
\value{
Invisibly returns the file path.
}
\description{
Writes the output of \code{\link[=seg_short_form_analysis]{seg_short_form_analysis()}} to a formatted
Excel workbook with native Excel line charts. Creates a single sheet with:
\itemize{
\item Title and project name header
\item Metric definitions (precision, recall, overall accuracy)
\item Optional segment legend with abbreviations and full names
\item Data table with merged headers, bordered, percentage-formatted
\item Precision and Recall line charts (via openpyxl/reticulate)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_utils.R
\name{deploy_update_runtime}
\alias{deploy_update_runtime}
\title{Update the bundled shinylive runtime}
\usage{
deploy_update_runtime(package_dir = ".")
}
\arguments{
\item{package_dir}{Character. Path to the work package source
directory. Defaults to the current working directory (assumes you're in
the package root).}
}
\value{
Invisibly returns the path to the updated runtime directory.
}
\description{
Refreshes the shinylive/webR runtime files stored in the package's
\verb{inst/deploy_template/runtime/} directory from the shinylive R package cache.
Run this after updating the shinylive R package to keep the launcher
runtime in sync.
}
\details{
The function locates the shinylive cache (populated by
\code{\link[shinylive:export]{shinylive::export()}}), then assembles an R-only runtime by:
\itemize{
\item Rendering the mustache \code{index.html} template into concrete HTML
\item Copying \code{shinylive-sw.js} and \verb{edit/} directory
\item Copying \verb{shinylive/} (excluding Python-specific files: \verb{pyodide/},
\verb{pyright/}, \code{examples.json})
}

The shinylive cache is located at:
\itemize{
\item macOS: \verb{~/Library/Caches/shinylive/shinylive-\{version\}/}
\item Linux: \verb{~/.cache/shinylive/shinylive-\{version\}/}
\item Windows: \verb{\%LOCALAPPDATA\%/Cache/shinylive/shinylive-\{version\}/}
}

If the cache does not exist, run \code{shinylive::export()} once on any Shiny
app to download and cache the assets.
}
\examples{
\dontrun{
# From the package root directory:
deploy_update_runtime()

# Or specify the package path explicitly:
deploy_update_runtime(package_dir = "~/Documents/GitHub/work")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_describe_solutions.R
\name{seg_describe_solutions}
\alias{seg_describe_solutions}
\title{seg_describe_solutions}
\usage{
seg_describe_solutions(
  seg,
  solution,
  where = NULL,
  polar_threshold = 0.2,
  profile_threshold = 0.15,
  n_polar = 15,
  n_profile = 10,
  model = "claude-sonnet-4-20250514",
  api_key = NULL,
  add_to_wb = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{seg}{A seg object with solutions written via \code{\link[=seg_write_solutions]{seg_write_solutions()}}.}

\item{solution}{Character. The \code{lda_name} of the solution to describe
(e.g. \code{"LDA_opt_kmeans_source_eq_H7"}).}

\item{where}{Character. Path to the solutions directory. Defaults to
\code{seg[["paths"]][["folders"]][["solution"]]}.}

\item{polar_threshold}{Numeric. Minimum absolute diff for a polar hit
(default: \code{0.20}).}

\item{profile_threshold}{Numeric. Minimum absolute diff for a profile hit
(default: \code{0.15}).}

\item{n_polar}{Integer. Number of top polar differentiators to include per
segment in the prompt (default: \code{15}).}

\item{n_profile}{Integer. Number of top profile differentiators to include
per segment in the prompt (default: \code{10}).}

\item{model}{Character. Claude model to use (default:
\code{"claude-sonnet-4-20250514"}).}

\item{api_key}{Character. Anthropic API key. If \code{NULL} (default), retrieves
via \code{get_environment_key("ANTHROPIC_API_KEY")}.}

\item{add_to_wb}{Logical. If \code{TRUE}, appends a "Descriptions" sheet to the
solution Excel workbook (default: \code{FALSE}).}

\item{verbose}{Logical. Print descriptions to console (default: \code{TRUE}).}
}
\value{
A tibble with columns: \code{segment}, \code{name}, \code{description}, \code{n}, \code{pct},
\code{polar_hits}, \code{profile_hits}.
}
\description{
Generates AI-written segment descriptions by calling the
Claude API. For each segment in a solution, extracts the top
differentiating variables (polar and profile) from the written solution
Excel file, sends the data to Claude, and returns a tibble with a short
segment name and 3-sentence description per segment.
}

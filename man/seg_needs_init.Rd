% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_init.R
\name{seg_needs_init}
\alias{seg_needs_init}
\title{seg_needs_init}
\usage{
seg_needs_init(seg, item_labels, context_labels)
}
\arguments{
\item{seg}{A seg object from \code{\link[=seg_init]{seg_init()}}.}

\item{item_labels}{Character vector of need labels, in item order. Length
sets how many items the loop is expected to carry.}

\item{context_labels}{Character vector of context labels, in context order
(for a hearables study, the occasions).}
}
\value{
The seg object with a populated \code{needs} slot and class
\code{analytic_needs} prepended.
}
\description{
Extends a segmentation project onto the needs path. A needs
segmentation carries TWO units of analysis: the person (as in a standard
seg) and the person-context grid produced by an importance loop. This
function adds the \code{needs} slot that holds the loop definition, the derived
basis column names and the theme assignment, and tags the object so
downstream \verb{seg_needs_*} functions can find them.

Call it on a seg that has already been through \code{\link[=seg_init]{seg_init()}}. It creates no
folders and writes no files - the standard three-folder layout is reused.
}

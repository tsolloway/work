% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_ensure_connectivity.R
\name{bn_ensure_connectivity}
\alias{bn_ensure_connectivity}
\title{Ensure All Nodes Are Connected in a Bayesian Network}
\usage{
bn_ensure_connectivity(
  bn,
  df,
  algorithm = c("tabu", "hc"),
  score = c("bic", "aic"),
  black_list = NULL,
  white_list = NULL,
  bootstrap_reps = 50,
  seed = 1
)
}
\arguments{
\item{bn}{A \code{bnlearn::bn} object.}

\item{df}{A data frame containing the variables in the network.}

\item{algorithm}{Character. Structure learning algorithm for bootstrap: \code{"tabu"} or \code{"hc"}.}

\item{score}{Character. Scoring function: \code{"bic"} or \code{"aic"}.}

\item{black_list}{Optional data frame of arcs to blacklist.}

\item{white_list}{Optional data frame of arcs to whitelist.}

\item{bootstrap_reps}{Integer. Number of bootstrap replicates for bridge arc selection.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A \code{bnlearn::bn} object with bridge arcs added to ensure full connectivity.
}
\description{
Checks whether the undirected skeleton of a Bayesian network is fully connected.
If disconnected components exist, bridges them by adding the strongest bootstrapped
arc between each pair of adjacent components.
}
\details{
The function:
\enumerate{
\item Converts the BN to an undirected igraph and checks connectivity.
\item If connected, returns the BN unchanged.
\item If disconnected, identifies components and bootstraps arc strengths across all
variables to find the strongest candidate arc between each pair of disconnected
components.
\item Adds the best bridge arc per component gap and verifies the result is connected.
}

This is the unsupervised analog to \code{\link[=bn_ensure_reachability]{bn_ensure_reachability()}}, which enforces
directed reachability to a dependent variable.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append_bn_prioritize_guide.R
\name{append_bn_prioritize_guide}
\alias{append_bn_prioritize_guide}
\title{append_bn_prioritize_guide}
\usage{
append_bn_prioritize_guide(
  wb,
  dv_display = NULL,
  has_brands = FALSE,
  has_weights = FALSE,
  has_subgroups = FALSE,
  has_strategy = FALSE,
  has_community = FALSE,
  sig_threshold = 0.05,
  marginal_threshold = 0.1,
  min_base_for_boot = 100,
  boot_applied = FALSE,
  n_boot_final = NULL,
  noise_tail = NULL,
  threshold = NULL,
  impact_shift_type = "headroom",
  meta = list(),
  add_prioritization_pvalue = FALSE
)
}
\arguments{
\item{wb}{An openxlsx workbook object.}

\item{dv_display}{Character or NULL. Display name of the DV for the header.}

\item{has_brands}{Logical. Whether a Focus (brand) control exists.}

\item{has_weights}{Logical. Whether a Weight control exists.}

\item{has_subgroups}{Logical. Whether a Subgroup control exists.}

\item{has_strategy}{Logical. Whether multiple strategies (lift/max) exist.}

\item{has_community}{Logical. Whether community labels appear in the table.}

\item{sig_threshold}{Numeric. P-value threshold for the "significant"
colour band.}

\item{marginal_threshold}{Numeric. P-value threshold for the "marginal"
colour band.}

\item{min_base_for_boot}{Integer. Minimum sample size used for the base /
warning feedback.}

\item{boot_applied}{Logical. Whether parameter-bootstrap replicates were
actually run (i.e., \code{n_boot_final > 1}).}

\item{n_boot_final}{Integer or NULL. Number of bootstrap replicates.}

\item{noise_tail}{Numeric or NULL. Fraction of tail steps used to estimate
the noise floor.}

\item{threshold}{Numeric or NULL. Early-stopping threshold (fraction).}

\item{add_prioritization_pvalue}{Logical. When TRUE AND the bootstrap
actually ran (\code{boot_applied}), the guide includes the
"Bootstrap p-values" and "Why compare to a noise floor" technical
sections. When FALSE (default) or when the bootstrap didn't run, both
sections are omitted (the reader never sees a p-value column either).}

\item{prioritize_display}{Character or NULL. Initial value of the
prioritization Display dropdown — \code{"Point Change"} or \code{"\% Change"}.
NULL (default) auto-detects from DV type: dichotomous -> "Point Change",
continuous -> "\% Change".}
}
\value{
The modified workbook object (invisibly).
}
\description{
Appends a client-friendly "Guide" sheet to an existing
\code{bn_prioritize_write()} workbook. Mirrors the format and tone of
\code{append_bn_impact_guide()}: a short client-facing section followed
by a technical appendix for statisticians. Added as the final tab.
}
\keyword{internal}

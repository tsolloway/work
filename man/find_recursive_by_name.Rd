% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_recursive_by_name.R
\name{find_recursive_by_name}
\alias{find_recursive_by_name}
\title{Recursively Search Nested Lists for an Object by Name}
\usage{
find_recursive_by_name(x, name, max_depth = Inf, depth = 0)
}
\arguments{
\item{x}{Object or list to search through.}

\item{name}{Character scalar. Name to search for (case-sensitive).}

\item{max_depth}{Positive integer or \code{Inf}. Maximum recursion depth.
Prevents infinite recursion in self-referential or excessively nested lists.
Defaults to \code{Inf}, meaning no limit.}

\item{depth}{Internal parameter used during recursion (do not modify).}
}
\value{
The first object whose name matches \code{name}, or \code{NULL} if not found
within the specified \code{max_depth}.
}
\description{
Recursively searches through a (potentially deeply nested) list
to find the first element whose \strong{name} matches a specified value.
This is useful for locating a named object (e.g., \code{"model"}, \code{"bn"}, \code{"network"})
in layered or complex result structures, regardless of its class.
}
\details{
The function performs a \strong{depth-first search}, inspecting the names
of list elements at each level until it finds one matching \code{name}.
\itemize{
\item \strong{Name-based search:} The function matches by element \strong{name}, not by class.
For example, if a list has \code{list(model = bnlearn::hc(iris))},
you can find it with \code{find_recursive_by_name(x, "model")} even if
the object class is not \code{"bn"}.
\item \strong{Depth control:} The \code{max_depth} argument prevents infinite recursion
in self-referential or highly nested lists. If \code{max_depth} exceeds the
actual nesting level, the function behaves normally.
\item \strong{Error safety:} Each recursive call is wrapped in \code{tryCatch()} to ensure
the function does not stop on malformed elements.
}

If no element with the specified name is found, the function returns \code{NULL}.
}
\examples{
\dontrun{
bn <- bnlearn::hc(iris)
nested <- list(a = list(b = list(model = bn)))

# Find the element named "model"
find_recursive_by_name(nested, name = "model")

# Limit search to depth 1 (will return NULL)
find_recursive_by_name(nested, name = "model", max_depth = 1)

# Will NOT match by class
find_recursive_by_name(nested, name = "bn")
}

}

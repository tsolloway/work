% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_progress.R
\name{map_progress}
\alias{map_progress}
\title{map_progress}
\usage{
map_progress(
  .x,
  .f,
  ...,
  .label = NULL,
  .handlers = progressr::handler_cli,
  .parallel = FALSE,
  .furrr_packages = NULL
)
}
\arguments{
\item{.x}{A vector, list, or other iterable to map over.}

\item{.f}{A function or formula to apply to each element.}

\item{...}{Additional arguments passed to \code{.f}.}

\item{.label}{Optional string or function producing per-iteration messages.
If a string, it is prefixed to each element name (e.g., \code{"Running subgroup"}).
If a function, it should accept \code{.x[[i]]} and return a message string.}

\item{.handlers}{A list of \code{progressr} handlers controlling progress output.
Defaults to \code{progressr::handler_cli}, which prints a progress bar on the left
and the message on the right.}

\item{.parallel}{Logical; if TRUE, use \code{furrr::future_map()} instead of
\code{purrr::map()}. Requires an active \code{future::plan()}.}

\item{.furrr_packages}{Optional character vector of packages to load in each worker.}
}
\value{
A list of mapped results, identical in structure to \code{purrr::map()}.
}
\description{
A safe wrapper around \code{purrr::map()} or \code{furrr::future_map()} that
displays progress updates via \code{progressr}. Each iteration updates a
progress bar and prints a contextual message (e.g., the current element
being processed).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append_bn_network_maps.R
\name{append_bn_network_maps}
\alias{append_bn_network_maps}
\title{append_bn_network_maps}
\usage{
append_bn_network_maps(
  wb,
  bn_full,
  network_type = "none",
  width = 10,
  height = 8,
  defer_images = FALSE,
  attribute_font_size = NULL,
  community_font_size = NULL
)
}
\arguments{
\item{wb}{An openxlsx workbook object.}

\item{bn_full}{The full output of \code{bn_finalize_network()}.}

\item{network_type}{Character. Layout type passed to \code{bn_visual()}.
One of \code{"none"} (default), \code{"gravity"}, \code{"charge"}, or
\code{"hierarchy"}.}

\item{width}{Numeric. Image width in inches. Default 10.}

\item{height}{Numeric. Image height in inches. Default 8.}

\item{defer_images}{Logical. When FALSE (default), images are inserted
directly via \code{openxlsx::insertImage}. When TRUE, the sheets are
created and PNGs are rendered to a temp dir, but images are NOT
inserted — metadata is attached to the workbook under attribute
\code{"network_map_images"} so the caller can insert via openxlsx2
after a save/reload. Used by \code{bn_write()}.}

\item{attribute_font_size}{Numeric or NULL. Node-label font size (in pixels)
for the Attribute Network PNG. NULL leaves vis.js's default 14 px.}

\item{community_font_size}{Numeric or NULL. Node-label font size (in pixels)
for the Community Network PNG. NULL leaves vis.js's default 14 px.}
}
\value{
The modified workbook object (invisibly).
}
\description{
Internal helper that renders Bayesian network maps as PNG
images and inserts them into an Excel workbook on separate sheets. Uses
\code{bn_visual(save_to_png = TRUE)} to capture visNetwork widgets.
}
\keyword{internal}

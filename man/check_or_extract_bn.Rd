% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_or_extract_bn.R
\name{check_or_extract_bn}
\alias{check_or_extract_bn}
\title{Validate or Extract a bnlearn Network from an Object or Nested List}
\usage{
check_or_extract_bn(obj, recursive = FALSE)
}
\arguments{
\item{obj}{Object to check or search.}

\item{recursive}{Logical; if \code{TRUE}, searches nested lists recursively
for a \code{bn} object. Default = \code{FALSE}.}
}
\value{
A \code{bnlearn::bn} object if found; otherwise, an error is raised.
}
\description{
Ensures the input is a \code{bnlearn::bn} object, or extracts the first \code{bn}
object found in a list (optionally nested). This is useful for safely
passing user inputs to functions that require a fitted Bayesian network.
}
\details{
\itemize{
\item If \code{obj} is already a \code{bnlearn::bn} object, it is returned as-is.
\item If \code{obj} is a list, the function uses \code{find_recursive()} to locate
the first element that inherits from \code{"bn"}.
\item If no Bayesian network object is found, the function stops with
an informative error message.
}

This function acts as a validation and extraction utility for any function
that expects a Bayesian network as input, making it robust to nested
return structures (e.g., lists of results from bootstrapping or modeling pipelines).
}
\examples{
\dontrun{
library(bnlearn)
bn <- bnlearn::hc(iris)

# Case 1: Direct BN
check_or_extract_bn(bn)

# Case 2: BN inside a simple list
check_or_extract_bn(list(model = bn))

# Case 3: Deeply nested BN
check_or_extract_bn(list(a = list(b = list(model = bn))), recursive = TRUE)
}

}

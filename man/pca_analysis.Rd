% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca_analysis.R
\name{pca_analysis}
\alias{pca_analysis}
\title{pca_analysis}
\usage{
pca_analysis(
  df,
  vars = NULL,
  labels = NULL,
  rotation = c("equamax", "varimax", "quartimax", "bentlerT", "varimin", "geominT",
    "bifactor", "Promax", "promax", "oblimin", "simplimax", "bentlerQ", "geominQ",
    "biquartimin", "none"),
  max_factors = 30,
  seed = 1,
  return_raw_analysis = FALSE
)
}
\arguments{
\item{df}{A data frame containing the variables to analyze.}

\item{vars}{Character vector of variable names. If \code{NULL}, uses all columns.}

\item{labels}{A data frame with \code{variable} and \code{label} columns, a named
character vector, or \code{NULL} for no labels.}

\item{rotation}{Character. Rotation method for \code{\link[psych:principal]{psych::principal()}} (default:
\code{"equamax"}).}

\item{max_factors}{Integer. Maximum number of factors to evaluate (default:
\code{30}). Set to \code{NULL} to evaluate all possible solutions.}

\item{seed}{Integer. Random seed for reproducibility (default: \code{1}).}

\item{return_raw_analysis}{Logical. If \code{TRUE}, includes the raw
\code{\link[psych:principal]{psych::principal()}} output objects (default: \code{FALSE}).}
}
\value{
A list containing \code{parameters}, \code{pca_tables} (one loading table per
factor count), and optionally \code{raw_analysis}.
}
\description{
Runs principal components analysis across a range of factor
counts (3 to n_vars - 2), applies the specified rotation, and returns
loading tables with factor assignments for each solution.
}

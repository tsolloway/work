% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_to_netviz_prep_for_communities.R
\name{bn_to_netviz_prep_for_communities}
\alias{bn_to_netviz_prep_for_communities}
\title{Prepare Community-Level Bayesian Network for visNetwork}
\usage{
bn_to_netviz_prep_for_communities(
  attribute_viz_prep,
  community_edge_by = c("sum", "mean")
)
}
\arguments{
\item{attribute_viz_prep}{A list containing \code{nodes} and \code{edges} from
\code{bn_to_netviz_prep()}.}

\item{community_edge_by}{One of \code{"sum"} or \code{"mean"}; how to aggregate
mutual information (\code{mi}) for edges between communities.}
}
\value{
A list with:
\describe{
\item{nodes}{Community-level nodes with \code{id}, \code{label}, and \code{value}.}
\item{edges}{Community-level edges with aggregated \code{mi}, \code{value}, and \code{index}.}
}
}
\description{
Aggregates a Bayesian network's nodes and edges into communities,
producing node and edge data suitable for \code{visNetwork} visualization.
}
\examples{
\dontrun{
library(bnlearn)
library(visNetwork)
data(learning.test)
bn <- hc(learning.test)

attribute_viz <- bn_to_netviz_prep(obj = bn, df = learning.test)
community_viz <- bn_to_netviz_prep_for_communities(
  attribute_viz_prep = attribute_viz$attribute_viz_prep,
  community_edge_by = "sum"
)
visNetwork(community_viz$nodes, community_viz$edges)
}
}

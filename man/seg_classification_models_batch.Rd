% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_classification_models_batch.R
\name{seg_classification_models_batch}
\alias{seg_classification_models_batch}
\title{Batch segment classification with reproducible parallel execution}
\usage{
seg_classification_models_batch(
  xs_vec,
  temp_seg_data,
  temp_db_data,
  n_folds = 5,
  rf_tune_length = 3,
  glmnet_tune_length = 10,
  rda_tune_length = 10,
  rf_ntree = 5,
  rf_mtry = NULL,
  rf_nodesize = NULL,
  seed = 1,
  mc.cores = parallel::detectCores(logical = FALSE),
  xs_seed_mode = c("same", "different"),
  folds_mode = c("same", "different")
)
}
\arguments{
\item{xs_vec}{Character vector of segment solution column names (e.g., \code{"seg_solution_1"}).}

\item{temp_seg_data}{Data frame containing \code{hVendorID} and each \code{xs} column.}

\item{temp_db_data}{Data frame of predictors keyed by \code{hVendorID}.}

\item{n_folds}{Positive integer. Number of CV folds.}

\item{rf_tune_length}{Positive integer. Number of RF \code{mtry} values to try when \code{rf_mtry} is \code{NULL}.}

\item{glmnet_tune_length}{Positive integer. Number of tuning values for glmnet.}

\item{rda_tune_length}{Positive integer. Number of tuning values for RDA.}

\item{rf_ntree}{Positive integer. Number of trees for random forest (\code{randomForest::randomForest(ntree=...)}).}

\item{rf_mtry}{Optional integer scalar. If provided, RF uses a fixed \code{mtry} (no tuning).}

\item{rf_nodesize}{Optional integer scalar. If provided, RF uses a fixed \code{nodesize}.}

\item{seed}{Integer scalar. Global seed controlling all deterministic randomness.}

\item{mc.cores}{Positive integer. Number of forked processes for \code{parallel::mclapply()}.}

\item{xs_seed_mode}{Character, one of \code{"same"} or \code{"different"}.
If \code{"different"}, each \code{xs} gets a deterministic unique base seed.}

\item{folds_mode}{Character, one of \code{"same"} or \code{"different"}.
If \code{"same"}, all model types within an \code{xs} share the same folds.
If \code{"different"}, each model type within an \code{xs} has deterministic different folds.}
}
\value{
A list with:
\describe{
\item{summaries}{A combined data frame with one row per \code{xs} x model type, including CV/training accuracy and RF hyperparameters used.}
\item{models}{Named list (by \code{xs}) of full per-\code{xs} results from \code{seg_classification_models}.}
\item{errors}{A data frame of failures with \code{xs}, \code{xs_seed}, and \code{error} message.}
\item{settings}{Batch settings (seed, modes, mc.cores).}
}
}
\description{
Runs \code{\link{seg_classification_models}} over many segment solution columns
(each element of \code{xs_vec}) using a reproducible parallel RNG stream.
You can choose whether each \code{xs} gets the same seed or a deterministic
different seed, and whether all model types share the same CV folds or use
deterministic different folds.
}
\details{
Reproducibility:
\itemize{
\item Parallel reproducibility is enforced via \code{RNGkind("L'Ecuyer-CMRG")}
and \code{mc.set.seed = TRUE}.
\item Seeds used for each \code{xs} and each model type are stored in the returned object.
}
}
\examples{
\dontrun{
tmp_seg <- dplyr::tibble(
  hVendorID = seq_len(nrow(iris)),
  Species  = iris[["Species"]]
)
tmp_db <- dplyr::tibble(
  hVendorID = seq_len(nrow(iris)),
  Sepal.Length = iris[["Sepal.Length"]],
  Sepal.Width  = iris[["Sepal.Width"]],
  Petal.Length = iris[["Petal.Length"]],
  Petal.Width  = iris[["Petal.Width"]]
)

out <- seg_classification_models_batch(
  xs_vec = "Species",
  temp_seg_data = tmp_seg,
  temp_db_data = tmp_db,
  n_folds = 5,
  rf_ntree = 100,
  rf_mtry = 2,
  rf_nodesize = 1,
  seed = 42,
  xs_seed_mode = "different",
  folds_mode = "same",
  mc.cores = 2
)
}

}

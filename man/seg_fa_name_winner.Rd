% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_fa_name_winner.R
\name{seg_fa_name_winner}
\alias{seg_fa_name_winner}
\title{seg_fa_name_winner}
\usage{
seg_fa_name_winner(
  seg,
  fa_winner,
  from_excel = TRUE,
  max_words = 2,
  min_words = 1,
  model = "claude-sonnet-4-5-20250929",
  api_key = NULL,
  row_header = 4,
  file_location = NULL,
  quietly = TRUE
)
}
\arguments{
\item{seg}{A seg object with the PCA file path populated by \code{\link[=seg_pca]{seg_pca()}}.}

\item{fa_winner}{Integer. The winning factor solution number (sheet name in
the PCA workbook).}

\item{from_excel}{Logical. If \code{TRUE} (default), reads loading data directly
from the PCA Excel workbook (matches the workflow where
\code{\link[=seg_get_fa_winner]{seg_get_fa_winner()}} has not been called yet). If \code{FALSE}, reads from
\code{seg[["input_sheet"]][["input_fa_table"]]} (must be populated by
\code{\link[=seg_get_fa_winner]{seg_get_fa_winner()}} first) and additionally updates that table's
\code{fa_name} column to mirror what's written to Excel.}

\item{max_words}{Integer. Maximum words per factor name (default \code{2}).}

\item{min_words}{Integer. Minimum words per factor name (default \code{1}).}

\item{model}{Character. Claude model ID (default
\code{"claude-sonnet-4-5-20250929"}).}

\item{api_key}{Character or \code{NULL}. Anthropic API key. If \code{NULL} (default),
reads from \code{ANTHROPIC_API_KEY} via \code{\link[=get_environment_key]{get_environment_key()}}.}

\item{row_header}{Integer. Row where the data header starts in the PCA
sheet (default: \code{4}).}

\item{file_location}{Character. Path to the PCA Excel file. Defaults to
\code{seg[["paths"]][["files"]][["pca"]]}.}

\item{quietly}{Logical. If \code{TRUE} (default), suppress all console output.
Set to \code{FALSE} to print per-factor evidence and the resulting names.}
}
\value{
The seg object. Names are always written to the PCA Excel workbook
at the \code{Name} column of the winning sheet. When \code{from_excel = FALSE}, the
\code{fa_name} column of \code{seg[["input_sheet"]][["input_fa_table"]]} is also
updated in place.
}
\description{
Uses Claude to name the factors of a winning PCA solution.
For each factor, sends Claude the variable labels that load on it
(sorted by absolute loading, with the signed loading shown so it can
weight by contribution) and asks for a concise 1-2 word thematic name.
The new names are written into the \code{Name} column of the winning sheet in
the PCA Excel workbook so they propagate through \code{\link[=seg_get_fa_winner]{seg_get_fa_winner()}}
and \code{\link[=seg_input_sheet]{seg_input_sheet()}}.
}

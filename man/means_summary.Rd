% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/means_summary.R
\name{means_summary}
\alias{means_summary}
\title{means_summary}
\usage{
means_summary(df_stack, stack_labels, weight = NULL)
}
\arguments{
\item{df_stack}{Stacked data frame (one row per respondent-brand).}

\item{stack_labels}{Named character vector of variable labels for the
variables to summarise (names = variable names in \code{df_stack}).}

\item{weight}{Name of a weight column in \code{df_stack}. If provided, means are
computed as weighted means via \code{stats::weighted.mean()}. Default \code{NULL}
(unweighted).}
}
\value{
A tibble with one row per variable, columns for total and per-brand
means and counts.
}
\description{
Compute brand-level and total means and counts for stacked
survey data. Returns a summary tibble with Variable, Label, Total,
Total - N, then interleaved brand mean and N columns.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_stack.R
\name{seg_needs_stack}
\alias{seg_needs_stack}
\title{seg_needs_stack}
\usage{
seg_needs_stack(
  seg,
  stem,
  id_var = "record",
  context_vars = NULL,
  sep = "_",
  item_sep = "r"
)
}
\arguments{
\item{seg}{A seg object from \code{\link[=seg_needs_init]{seg_needs_init()}} with data loaded.}

\item{stem}{Character. The loop stem, e.g. \code{"NeedsxOccasion"} for columns
named \code{NeedsxOccasion_3r7}.}

\item{id_var}{Character. Respondent identifier on the person frame, carried
onto every grid row as \code{person_id}.}

\item{context_vars}{Named character vector of per-context variables to carry
across, where the name is the output column and the value is a template
containing \code{{context}}, e.g.
\code{c(context_freq = "OccasionFrequencyr{context}")}.}

\item{sep}{Character between the stem and the context index (default \code{"_"}).}

\item{item_sep}{Character between the context index and the item index
(default \code{"r"}).}
}
\value{
The seg object with \code{seg[["data"]][["stacked"]]} populated and the
loop definition recorded in \code{seg[["needs"]][["loop"]]}.
}
\description{
Unstacks an importance loop into one row per person-context.
A loop written as \verb{<stem>_<context>r<item>} holds a full item grid for each
context a respondent was assigned, and NA for every context they were not.
This turns that wide block into a long frame where the unit is the grid.

A row is emitted only where the grid was actually answered, so the result
can never contain a grid of missing items. Person-level variables are
carried through unchanged and therefore repeat across a respondent's rows;
\code{id_var} is preserved so a grid-level solution can be rolled back up.

Always read \code{\link[=seg_needs_stack_report]{seg_needs_stack_report()}} before trusting the result. A loop
keyed by POSITION rather than by context identity will stack without
complaint and produce a plausible file of nonsense.
}

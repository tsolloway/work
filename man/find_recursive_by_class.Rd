% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_recursive_by_class.R
\name{find_recursive_by_class}
\alias{find_recursive_by_class}
\title{Recursively Search Nested Lists for a Specific Object Type}
\usage{
find_recursive_by_class(x, what = "bn", max_depth = Inf, depth = 0)
}
\arguments{
\item{x}{Object or list to search through.}

\item{what}{Character scalar. Class name to search for
(e.g., \code{"bn"} or \code{"bn.fit"}).}

\item{max_depth}{Positive integer or \code{Inf}. Maximum recursion depth.
Prevents infinite recursion in self-referential or excessively
nested lists. Defaults to \code{Inf}, meaning no limit.}

\item{depth}{Internal parameter used during recursion (do not modify).}
}
\value{
The first object found that inherits from the specified class,
or \code{NULL} if no such object exists within the specified \code{max_depth}.
}
\description{
Recursively searches through a (potentially deeply nested) list
to find the first element that inherits from a specified class.
This is useful for extracting a specific object (e.g., \code{"bn"}, \code{"bn.fit"},
\code{"igraph"}) from complex or layered result structures.
}
\details{
The function performs a \strong{depth-first search}, exploring each element
of the list recursively until it finds an object whose class inherits
from the specified value of \code{what}.
\itemize{
\item \strong{Class-based search:} The function matches objects by class
(via \code{inherits()}), \emph{not} by name. For example, if an element is named
\code{"bn"}, that alone does not qualify; it must actually be an object of
class \code{"bn"}.
\item \strong{Depth control:} The \code{max_depth} argument controls how far the function
can recurse. This prevents infinite recursion in cases of cyclic references
or extremely deep nesting. If \code{max_depth} is larger than the actual list
depth, the function behaves normally and simply completes without hitting
the limit.
\item \strong{Error safety:} Each recursive call is wrapped in a \code{tryCatch()} to avoid
stopping on malformed or inaccessible elements.
}

If no matching object is found, the function returns \code{NULL}.
}
\examples{
\dontrun{
library(bnlearn)

bn <- bnlearn::hc(iris)
nested <- list(a = list(b = list(model = bn)))

# Find the first bn object in the nested list
find_recursive_by_class(nested, what = "bn")

# Limit search to depth 1 (will return NULL)
find_recursive_by_class(nested, what = "bn", max_depth = 1)

# This will NOT match by name only
fake <- list(bn = "not_a_bn_object")
find_recursive_by_class(fake, what = "bn") # returns NULL
}

}

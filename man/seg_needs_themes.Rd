% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_themes.R
\name{seg_needs_themes}
\alias{seg_needs_themes}
\title{seg_needs_themes}
\usage{
seg_needs_themes(
  seg,
  winner = NULL,
  nfactors = NULL,
  manual = NULL,
  move = NULL,
  theme_names = NULL,
  min_loading = 0.4,
  cross_gap = 0.1,
  level = c("person", "grid"),
  rotation = "equamax",
  file_location = NULL,
  row_header = 4
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_prepare]{seg_needs_prepare()}}.}

\item{winner}{Integer. Sheet of the rotation workbook to read, as picked on
review. Uses \code{seg$paths$files$pca} unless \code{file_location} is given.}

\item{nfactors}{Integer. Fit the structure here instead of reading a
workbook. Ignored when \code{winner} or \code{manual} is supplied.}

\item{manual}{Named list, theme name -> items. Items may be given as index,
variable name (\code{need01}, \code{pneed01}, \code{needs01}) or label.}

\item{move}{Named character, item -> theme name (or \code{NA} to leave the item
out of every theme). Applied after the source.}

\item{theme_names}{Character, one per theme in factor order. Defaults to
\code{"Theme 1"}, \code{"Theme 2"}, ...}

\item{min_loading}{Numeric. Below this an item loads on no theme
(default \code{0.40}).}

\item{cross_gap}{Numeric. A runner-up loading within this of the best marks
a cross-loader (default \code{0.10}).}

\item{level, rotation}{Used only with \code{nfactors}; as in
\code{\link[=seg_needs_structure]{seg_needs_structure()}}. The fit is PCA, matching the workbook.}

\item{file_location}{Character. Rotation workbook to read with \code{winner}.}

\item{row_header}{Integer. Header row of the workbook sheet (default \code{4}).}
}
\value{
The seg object with the assignment table in
\code{seg[["needs"]][["themes"]]}. Prints the table and a verdict.
}
\description{
Assigns each need to a theme - the unit \code{\link[=seg_needs_type]{seg_needs_type()}}
scores every grid on. Each item goes to the factor it loads on most
strongly, and the table comes back for review before anything is typed.

Three sources, in order of precedence:

\describe{
\item{\code{manual}}{a named list, theme name -> items. Replaces the loadings
outright.}
\item{\code{winner}}{the sheet of the rotation workbook written by
\code{\link[=seg_needs_structure]{seg_needs_structure()}} - the solution that was actually reviewed. This
is the normal route.}
\item{\code{nfactors}}{fit the structure here. Equamax has local optima, so a
fresh fit can move a marginal item relative to the workbook; prefer
\code{winner} once a solution has been picked.}
}

\code{move} then reassigns individual items on top of whichever source was
used, so a solution that is right bar one item does not have to be
rewritten in full.

Two things are flagged rather than silently resolved. An item whose best
loading is below \code{min_loading} loads on no theme; it is left out of the
theme scores, because forcing it in makes the theme mean partly noise. An
item whose runner-up loading is within \code{cross_gap} of its best is a
cross-loader; it stays assigned, but its theme is less clean than the table
makes it look.
}

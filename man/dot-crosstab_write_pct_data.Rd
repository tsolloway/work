% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_shell_add_crosstab.R
\name{.crosstab_write_pct_data}
\alias{.crosstab_write_pct_data}
\title{Write a percentage panel (row \% or col \%) entirely as formulas that
reference the counts panel at \code{counts_coords}. Inner cells divide a counts
cell by the appropriate marginal total; Total rows/cols are computed via
SUM of the inner percentages (= 1) or via the counts marginals (= marginal
share of grand total) depending on direction.}
\usage{
.crosstab_write_pct_data(wb, sheet_name, panel_type, coords, counts_coords)
}
\description{
Write a percentage panel (row \% or col \%) entirely as formulas that
reference the counts panel at \code{counts_coords}. Inner cells divide a counts
cell by the appropriate marginal total; Total rows/cols are computed via
SUM of the inner percentages (= 1) or via the counts marginals (= marginal
share of grand total) depending on direction.
}
\keyword{internal}

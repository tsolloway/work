% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_cut.R
\name{seg_needs_cut}
\alias{seg_needs_cut}
\title{seg_needs_cut}
\usage{
seg_needs_cut(
  seg,
  decisive_only = FALSE,
  max_themes = 2,
  min_n = 30,
  flat_cells = TRUE,
  flat_labels = c(top = "Everything matters", other = "Nothing stands out")
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_type]{seg_needs_type()}}.}

\item{decisive_only}{Logical. Build the cut from decisively typed grids only
(default \code{FALSE}). Near-tie grids then do not count toward a respondent's
themes, so more respondents fall to unclassified - read
\code{\link[=seg_needs_breadth]{seg_needs_breadth()}} both ways first.}

\item{max_themes}{Integer. Largest theme combination given its own cells;
anything broader is "Varied" (default \code{2}).}

\item{min_n}{Integer. Cells smaller than this are flagged (default \code{30}).}

\item{flat_cells}{Logical. Give respondents flat in every context - or in
most, with fewer than two typed - their own cells by rating level
(default \code{TRUE}). \code{FALSE} leaves them unclassified.}

\item{flat_labels}{Character, the two flat cells' labels:
\code{top} (every need at the top code) and \code{other}.}
}
\value{
The seg object with \code{seg[["needs"]][["cut"]]}: \code{persons} (one row
per respondent) and \code{cells} (the cell key). Prints the cell sizes and a
verdict.
}
\description{
The person-level typology: one cut that carries both what a
respondent needs and how consistently they need it. Each respondent is
placed by the set of themes their typed contexts fall into:

\describe{
\item{single}{every typed context in one theme - one cell per theme}
\item{pair}{exactly two themes - one cell per pair}
\item{...}{and so on up to \code{max_themes}}
\item{varied}{more than \code{max_themes} themes - one cell}
}

With 3 contexts asked and the default \code{max_themes = 2}, that is singles +
pairs + fully varied: 4 themes give 4 + 6 + 1 = 11 cells. More contexts
per respondent would let people show three themes without being fully
varied, and \code{max_themes = 3} names those triples - but the cell count
grows combinatorially (4 themes: 14 cells before the varied group; 5
themes: 25), so check the sizes this prints before committing.

A respondent with fewer than two typed contexts cannot show consistency
either way and is left unclassified (code \code{NA}), which is how
\code{\link[=seg_write_shell]{seg_write_shell()}} drops a respondent from a solution.

\strong{Respondents flat in every context get cells of their own.} Their
grids carry no need shape, so no typing can place them - but they are
real respondents with one clear, consistent answer, and on Kadro every one
of them answered at the same level in every context. With
\code{flat_cells = TRUE} they are split by that level: every need at the top
code is "Everything matters"; any lower level is "Nothing stands out".
They are level segments, not needs segments, and they ran faster than the
sample, so profile them before presenting them.

A respondent who is flat in MOST contexts and has fewer than two typed
joins them, by the level of their flat contexts. On Kadro those were 76
respondents with two flat contexts and one typed, and the one typed context
went to Smart Utility 71\% of the time against 26\% overall - the near-flat
pull, typing a nearly straight-lined grid by rating level. So they are
level respondents with one near-flat context, and a "Smart Utility only"
cell would be the wrong home. With this the Kadro flat cells hold 209
respondents (133 flat everywhere, 76 flat in most) and none are left
unclassified. \code{flat_basis} on the person table records which applied.

Breadth is censored: a "single" respondent is single on the contexts
observed. Label deliverables accordingly.
}

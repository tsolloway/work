% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_move_attribute_to.R
\name{bn_move_attribute_to}
\alias{bn_move_attribute_to}
\title{bn_move_attribute_to}
\usage{
bn_move_attribute_to(obj, moves)
}
\arguments{
\item{obj}{A \code{bn} engine result (or anything containing an
\code{attribute_viz_prep} with \code{nodes} and \code{edges}).}

\item{moves}{A data frame with column \code{id} plus either \code{community_name},
\code{group}, or both, naming where each attribute should move.}
}
\value{
A list with three elements:
\itemize{
\item \code{white_list}: data frame of surviving arcs (no row touches a moved id).
\item \code{black_list}: data frame of arcs to forbid (each moved id paired with
every node outside its target community, in both directions).
\item \code{manual_groups}: updated nodes data frame with new community assignments
applied to the moved ids.
}
}
\description{
Reassigns one or more attributes (nodes) to new communities and produces
the inputs needed to re-fit the network with those moves enforced.

For each moved attribute, the function:
\enumerate{
\item Strips every arc touching that attribute from the current edges, leaving
a \code{white_list} of surviving arcs that the next fit will preserve.
\item Adds blacklist entries banning the attribute from connecting to any node
outside the target community, forcing the engine to re-discover arcs
only within that community.
\item Updates the \code{manual_groups} table so the attribute carries the new
\code{community_name}, \code{group}, and \code{color}.
}

Pass either \code{community_name} or \code{group} (or both) in \code{moves} — the missing
column is resolved against the existing community lookup in \code{obj}.
}
\section{Validation}{

\itemize{
\item \strong{Unknown \code{id}} — hard error listing the offending ids. The function will
not silently drop a node it cannot find in the network.
\item \strong{Unknown \code{community_name}} — \code{cli::cli_warn()} listing each invalid
\verb{(id → community_name)} pair alongside the valid set; those rows are
skipped and the rest of the moves still apply.
\item \strong{Unknown \code{group}} — hard error (group numbers are unambiguous, so a
typo there is treated as a programming bug rather than a stakeholder one).
\item \strong{All moves invalid} — emits a warning and returns the original
\code{manual_groups}, an empty \code{black_list}, and \code{white_list} equal to the
current edges (i.e. a no-op).
}
}

\examples{
\dontrun{
moves <- tibble::tribble(
  ~id,        ~community_name,
  "q14b_46",  "Right Format",
  "q14a_12",  "Parental Support",
  "q14b_8",   "Brand Trust"
)

out <- bn_previous$cb_direct$brand_preference_tb \%>\%
  bn_move_attribute_to(moves)

bn_initial_networks(
  df            = df_impute,
  dv            = "brand_preference_tb",
  ivs           = iv_batteries,
  white_list    = out$white_list,
  black_list    = out$black_list,
  manual_groups = out$manual_groups
)
}

}

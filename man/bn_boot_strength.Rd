% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_boot_strength.R
\name{bn_boot_strength}
\alias{bn_boot_strength}
\title{Bootstrap Bayesian Network Strength with Directional Alignment}
\usage{
bn_boot_strength(
  df,
  bootstrap_reps = 100,
  algorithm = c("tabu", "hc", "tree.bayes"),
  score = c("bic", "aic"),
  maxp = 1,
  black_list = NULL,
  white_list = NULL,
  set_dif_arcs_df = NULL,
  evaluate_only_arcs_df = NULL,
  align_direction = TRUE,
  threshold = NULL,
  auto_threshold_ratio = 1/4,
  strength_min = 0.01,
  return_direction = TRUE,
  seed = 1
)
}
\arguments{
\item{df}{A data frame containing the variables to fit the network.}

\item{bootstrap_reps}{Integer. Number of bootstrap replicates (default = 100).}

\item{algorithm}{Character. Structure learning algorithm to use: \code{"tabu"}, \code{"hc"}, or \code{"tree.bayes"}.}

\item{score}{Character. Scoring function for structure learning: \code{"bic"} or \code{"aic"}.}

\item{maxp}{Integer. Maximum number of parents per node (used by \code{"hc"} and \code{"tabu"} algorithms).}

\item{black_list}{Optional data frame of arcs to blacklist (exclude) during structure learning.}

\item{white_list}{Optional data frame of arcs to whitelist (force include) during structure learning.}

\item{set_dif_arcs_df}{Optional data frame of arcs (and their reverses) to exclude from the final bootstrap output.}

\item{evaluate_only_arcs_df}{Optional data frame of arcs (and their reverses) to specifically evaluate from the bootstrap results.}

\item{align_direction}{Logical; if \code{TRUE}, arcs are oriented to match the majority-supported direction.}

\item{threshold}{Numeric or \code{"auto"}. Minimum strength to retain arcs, or \code{"auto"} to select top arcs based on \code{auto_threshold_ratio}.}

\item{auto_threshold_ratio}{Numeric. Proportion of arcs to retain if \code{threshold = "auto"} (default = 1/4).}

\item{strength_min}{Numeric. Minimum strength below which arcs are removed (default = 0.01).}

\item{return_direction}{Logical. If \code{TRUE}, includes the \code{direction} column in the returned data.}

\item{seed}{Optional integer for reproducibility. If NULL, no seed is set.}
}
\value{
A data frame of arcs with columns:
\itemize{
\item \code{from}: source node
\item \code{to}: target node
\item \code{strength}: bootstrap-estimated strength of the arc
\item \code{direction}: proportion of times the arc points in this direction (if \code{return_direction = TRUE})
}
}
\description{
Performs bootstrap estimation of Bayesian network arc strengths using
\code{bnlearn::boot.strength()}, with optional directional alignment
(flipping arcs to match majority direction), thresholding, and filtering
of specific arcs or their reverses.
}
\examples{
\dontrun{
bn_boot_strength(df = my_data, bootstrap_reps = 200, algorithm = "tabu",
                 threshold = 0.6, align_direction = TRUE)
}

}

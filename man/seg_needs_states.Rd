% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_states.R
\name{seg_needs_states}
\alias{seg_needs_states}
\title{seg_needs_states}
\usage{
seg_needs_states(
  seg,
  k = 4:8,
  adopt = NULL,
  state_names = NULL,
  basis = c("centred", "centred_scaled", "scaled"),
  tie_margin = NULL,
  nstart = 20,
  iter_max = 100,
  seed = 1
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_prepare]{seg_needs_prepare()}}.}

\item{k}{Integer vector of state counts to compare (default \code{4:8}).}

\item{adopt}{Integer. Make this k's solution the typing. Uses the stored
run if it covers this k and basis, otherwise runs it.}

\item{state_names}{Character, one per state, used with \code{adopt}. Defaults to
a placeholder naming the need each state over-indexes on most.}

\item{basis}{Character. \code{"centred"} (default) grid-centres the answers;
\code{"centred_scaled"} also z-scores each item; \code{"scaled"} only z-scores -
kept for comparison, and it clusters on rating level.}

\item{tie_margin}{Numeric or \code{NULL}. Relative margin below which a grid is
a near-tie. \code{NULL} (default) takes the 10th percentile of the margins for
each k, as \code{\link[=seg_needs_type]{seg_needs_type()}} does. The cut-off, share flagged and margin
quartiles are printed on adopt and stored in the typing.}

\item{nstart, iter_max, seed}{Passed to \code{\link[=cluster_kmeans]{cluster_kmeans()}}.}
}
\value{
The seg object with the run in \code{seg[["needs"]][["states"]]} and,
with \code{adopt}, the typing replaced. Prints a comparison and a verdict.
}
\description{
The richer alternative to theme typing: cluster the grids into
k need states on their whole need profile, rather than typing each grid to
the one theme it leans toward most. A state can be a mixture - high on
Escape and Frictionless together - which theme typing can only call a
near-tie.

Runs \code{\link[=cluster_kmeans]{cluster_kmeans()}} over a range of k and prints a comparison to
choose from. Nothing downstream changes until a k is adopted:
\code{adopt = 5} makes that solution the typing, in the same slot and the same
shape as \code{\link[=seg_needs_type]{seg_needs_type()}} writes, so \code{\link[=seg_needs_breadth]{seg_needs_breadth()}},
\code{\link[=seg_needs_cut]{seg_needs_cut()}} and the rest run on states unchanged. Re-running
\code{\link[=seg_needs_type]{seg_needs_type()}} puts the themes back.

\strong{The basis is grid-centred by default.} Clustering is
distance-based, so rating generosity is an axis the clusters will split on
unless it is removed. On Kadro, k-means on item-scaled needs produced
states that were largely rating bands - intensity explained 71\% of state
membership at k = 4 - against 7.5\% on the grid-centred basis, level with
the theme typing. This is the one place in the needs path where centring
earns its keep.

\strong{Flat grids are left out} for the same reason as in
\code{\link[=seg_needs_type]{seg_needs_type()}}: centred, they are all the zero vector, so they would
land together in whichever state sits nearest the origin.

\strong{Near-ties.} A grid almost equidistant between two centroids is
the state analogue of a near-tie. The margin is \code{1 - d1 / d2}, the nearest
centroid distance over the second-nearest: 0 is equidistant, 1 is on the
centroid. The cut-off follows the same rule as \code{\link[=seg_needs_type]{seg_needs_type()}}: by
default the closest-called 10\% of grids are flagged, so a "close call"
means the same share of grids for states as for themes and the
\code{decisive_only} readings of the two are comparable. (A fixed 0.10 flagged
a third of Kadro grids at k = 4, against an eighth for the themes.)

There is no natural k. On the Kadro partial, silhouette beat a null with
the same covariance by a flat margin at every k from 2 to 8, so k is chosen
on interpretability, cell balance and \code{\link[=seg_needs_state_lock]{seg_needs_state_lock()}}, not on fit.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_write_solutions.R
\name{seg_needs_write_solutions}
\alias{seg_needs_write_solutions}
\title{seg_needs_write_solutions}
\usage{
seg_needs_write_solutions(
  seg,
  k = NULL,
  cut_args = list(),
  file_label = "Needs",
  comparison_file = "Needs Solutions Comparison",
  ...
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_themes]{seg_needs_themes()}} and \code{\link[=seg_get_spec]{seg_get_spec()}},
with the grid as the unit.}

\item{k}{Integer vector of cluster solutions to include. Default: every k
stored by the last \code{\link[=seg_needs_cluster]{seg_needs_cluster()}} run; none if it has not been
run.}

\item{cut_args}{List of arguments for \code{\link[=seg_needs_cut]{seg_needs_cut()}}, applied to every
solution (default: its defaults).}

\item{file_label}{Character. Prefix for the shell file names
(default \code{"Needs"}), giving e.g. "Needs Themes Person", "Needs Clusters
k4 Grid".}

\item{comparison_file}{Character. File name of the comparison workbook.}

\item{...}{Passed through to \code{\link[=seg_write_shell]{seg_write_shell()}} (e.g. \code{where}).}
}
\value{
The seg object, invisibly, unchanged except for the comparison in
\code{seg[["needs"]][["reports"]][["solutions"]]}. Prints the comparison.
}
\description{
Writes every candidate needs solution to the solutions folder
so they can be reviewed side by side: the factor solution (themes, from
\code{\link[=seg_needs_type]{seg_needs_type()}}) and each cluster solution (need states, from
\code{\link[=seg_needs_cluster]{seg_needs_cluster()}}). For each it writes the person and grid shells with
\code{\link[=seg_needs_write_cut]{seg_needs_write_cut()}}, and it writes one comparison workbook that puts
the solutions head to head on the measures that decide between them.

Each solution is built on its own copy of the seg object, so the typing
and cut on \code{seg} are left exactly as they were - reviewing the clusters
does not switch the project onto them. Adopt the winner deliberately
afterwards (\code{seg_needs_cluster(adopt = )}, or keep the themes).

Every solution is cut the same way (\code{cut_args}), and the cluster solutions
flag the same share of close calls as the themes, so the numbers compare
like with like.
}

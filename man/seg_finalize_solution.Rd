% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_finalize_solution.R
\name{seg_finalize_solution}
\alias{seg_finalize_solution}
\title{seg_finalize_solution}
\usage{
seg_finalize_solution(
  seg,
  final_solution_name,
  items,
  overwrite = FALSE,
  segment_names = NULL,
  qualification_instructions = rep("qualification goes here", 8),
  survey_respondent_id = NULL,
  truncate = c("no", "yes", "both"),
  weighted = TRUE,
  version = c("traditional", "both"),
  strategy = c("multisession", "multicore", "sequential", "cluster"),
  workers = NULL,
  setting_polar_threshold = 0.2,
  setting_profile_threshold = 0.15,
  seed = 1
)
}
\arguments{
\item{seg}{A seg object with a prepped family in
\code{seg$solutions$analysis[[final_solution_name]]} (run
\code{\link[=seg_finalize_prep_solution]{seg_finalize_prep_solution()}} first) and a shell (\code{\link[=seg_do_spec]{seg_do_spec()}}).}

\item{final_solution_name}{Character. Name of the prepped family to finalize
from (e.g. \code{"Solution_A"}). The finalized solution is named
\code{"{final_solution_name}_Final"}.}

\item{items}{Integer. The item count to finalize — selects the prepped
solution whose LDA input set has exactly this many variables.}

\item{overwrite}{Logical. If \code{FALSE} (default) and the finalized solution /
output folder already exists, the function stops. If \code{TRUE}, the existing
output folder and registered rows/columns are removed first.}

\item{segment_names, qualification_instructions}{Passed through to
\code{\link[=seg_typing_tool]{seg_typing_tool()}}.}

\item{survey_respondent_id}{Character. Respondent ID column for the typing
tool. Defaults to the seg object's respondent id (\code{\link[=get_resp_id_name]{get_resp_id_name()}}).}

\item{truncate, weighted, version, strategy, workers}{Passed through to
\code{\link[=seg_write_shell_parallel]{seg_write_shell_parallel()}} for the solution shell.}

\item{setting_polar_threshold, setting_profile_threshold}{Numeric (defaults
\code{0.2} / \code{0.15}). Significance/colouring thresholds for polar and profile
variables, passed through to \code{\link[=seg_write_shell_parallel]{seg_write_shell_parallel()}}.}

\item{seed}{Numeric. Random seed for the independent LDA refit. Defaults to
\code{1} (matching \code{\link[=seg_short_form_analysis]{seg_short_form_analysis()}}).}
}
\value{
The seg object, invisibly, with the finalized solution registered
under \code{seg$solutions$analysis[["{final_solution_name}_Final"]]} and
\code{summary_table} / \code{with_solutions} rebuilt.
}
\description{
Finalizes the single selected solution out of a prepped family
produced by \code{\link[=seg_finalize_prep_solution]{seg_finalize_prep_solution()}}. Given a \code{final_solution_name}
(the prepped family) and an \code{items} count, this:
\enumerate{
\item picks the prepped solution at that item count;
\item regenerates the LDA independently from the seg object (data,
inputs, cluster target, prior, seed) and verifies the resulting segment
assignment matches the prepped solution exactly — stopping if it does
not, so the shipped deliverable is provably reproducible;
\item registers it as \code{"{final_solution_name}_Final"} on the seg object;
\item writes a single shell and typing tool into
\verb{3. Solutions/FINAL-SOLUTIONS/\{final_solution_name\} - Final/}.
}
}

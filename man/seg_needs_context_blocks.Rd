% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_context_blocks.R
\name{seg_needs_context_blocks}
\alias{seg_needs_context_blocks}
\title{seg_needs_context_blocks}
\usage{
seg_needs_context_blocks(
  seg,
  nets = NULL,
  freq_var = NULL,
  freq_cuts = NULL,
  flags = NULL,
  noun = "context",
  prefixes = c(which = "OA", nets = "OG", freq = "OQ"),
  block_names = NULL
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_stack]{seg_needs_stack()}}.}

\item{nets}{Named list, net label -> contexts (indices or context labels),
e.g. \code{list("Any exercise" = c(7, 8))}. \code{NULL} skips the nets block.}

\item{freq_var}{Character. The per-context frequency column, as named in
\code{context_vars} at stack time. Defaults to the only one when there is only
one.}

\item{freq_cuts}{Named character, row label -> value in spec grammar, e.g.
\code{c("Does this weekly or more" = ">= 4")}. When \code{NULL}, one row per coded
value is written from the column's value labels, if it has them.}

\item{flags}{Named character, per-context 0/1 column -> row label, e.g.
\code{c(context_is_top = "One of the respondent's most frequent")}. Added to
the frequency block.}

\item{noun}{Character. What a context is called in block names
(default \code{"context"}; \code{"occasion"} for an occasions loop).}

\item{prefixes}{Named character: \code{which}, \code{nets}, \code{freq}.}

\item{block_names}{Named character overriding any of the default names.}
}
\value{
A list of profile blocks, as from \code{\link[=seg_create_profile_block]{seg_create_profile_block()}}.
}
\description{
Writes the per-context profile blocks for a needs spec from the
stack map, so they are not hand-written for each project:

\describe{
\item{which}{one row per context - which context this grid is}
\item{nets}{context groupings, from \code{nets}}
\item{frequency}{how often the respondent does this context, from a
per-context variable carried by \code{\link[=seg_needs_stack]{seg_needs_stack()}}, plus any 0/1
per-context flags}
}

All three are grid-level: they read \code{context} and the \code{context_vars}
columns, which differ down a respondent's rows. \code{\link[=seg_needs_write_shell]{seg_needs_write_shell()}}
detects that and keeps them off the person shell.

Unlike the other \verb{seg_needs_*} functions this returns blocks, not the seg
object - it feeds \code{\link[=seg_generate_spec]{seg_generate_spec()}}, which runs before the spec
exists.
}

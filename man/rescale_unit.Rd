% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rescale_unit.R
\name{rescale_unit}
\alias{rescale_unit}
\title{rescale_unit}
\usage{
rescale_unit(x, range)
}
\arguments{
\item{x}{Numeric vector.}

\item{range}{Numeric vector whose min and max are the scale bounds, e.g.
\code{1:3} or \code{c(1, 5)}.}
}
\value{
\code{x} on 0-1. Values outside \code{range} are returned outside 0-1 rather
than clipped, so a miscoded answer is visible instead of silently absorbed.
}
\description{
Maps a scale onto 0-1 linearly: \code{(x - lo) / (hi - lo)}. On a
3-point importance scale with \code{range = 1:3} that is 1 -> 0, 2 -> 0.5,
3 -> 1.

The bounds are given rather than taken from the data on purpose. A spec is
a record of how a shell variable is built, so the same spec row must
produce the same value on a partial file and on the final one - and an
item nobody rated at the floor would otherwise rescale differently from
its neighbours on the same battery.
}

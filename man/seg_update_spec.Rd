% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_update_spec.R
\name{seg_update_spec}
\alias{seg_update_spec}
\title{seg_update_spec}
\usage{
seg_update_spec(
  seg,
  spec_path = NULL,
  resp_id = NULL,
  execute_debug = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{seg}{A seg object that has already been through \code{\link[=seg_get_spec]{seg_get_spec()}} and
optionally \code{\link[=seg_cluster_with_profiles]{seg_cluster_with_profiles()}}.}

\item{spec_path}{Character. Path to the spec Excel file. If \code{NULL}, uses the
path stored in \code{seg$paths$files$spec}.}

\item{resp_id}{Character. Name of the respondent ID column. If \code{NULL},
falls back to \code{seg$meta$id_variable}.}

\item{execute_debug}{Logical. If \code{TRUE}, prints debug info during spec
execution (default: \code{FALSE}).}

\item{verbose}{Logical. If \code{TRUE}, prints summary of changes (default: \code{TRUE}).}
}
\value{
The seg object with updated spec, shell, and data objects.
}
\description{
Re-reads and re-executes the segmentation spec, updating shell
variables in \code{seg$data$with_shell} and \code{seg$data$with_solutions} without
destroying other columns (e.g., solution assignments, okay_filter).
}

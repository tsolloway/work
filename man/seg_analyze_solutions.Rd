% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_analyze_solutions.R
\name{seg_analyze_solutions}
\alias{seg_analyze_solutions}
\title{seg_analyze_solutions}
\usage{
seg_analyze_solutions(
  seg,
  where = NULL,
  polar_threshold = 0.2,
  profile_threshold = 0.15,
  top_n = 10,
  only_opt = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{seg}{A seg object with solutions written via \code{\link[=seg_write_solutions]{seg_write_solutions()}}.}

\item{where}{Character. Path to the solutions directory. Defaults to
\code{seg[["paths"]][["folders"]][["solution"]]}.}

\item{polar_threshold}{Numeric. Minimum absolute diff for a polar variable
to count as a "hit" (default: \code{0.20}).}

\item{profile_threshold}{Numeric. Minimum absolute diff for a profile
variable to count as a "hit" (default: \code{0.15}).}

\item{top_n}{Integer. Number of top solutions to return per perspective
(default: \code{10}).}

\item{only_opt}{Logical. If \code{TRUE}, only analyze reduced-inputs (LDA_opt_)
solutions (default: \code{FALSE}).}

\item{verbose}{Logical. Print ranked tables to console (default: \code{TRUE}).}
}
\value{
A named list with elements \code{p1} through \code{p5} (tibbles), \code{metrics}
(full metrics table), and \code{details} (per-solution parsed data).
}
\description{
Analyzes all written solution files and ranks them under five
perspectives. Each perspective returns a tibble of the top solutions with
relevant metrics. The function reads the Excel files previously written by
\code{\link[=seg_write_solutions]{seg_write_solutions()}} and extracts summary-sheet and per-segment data.
\subsection{Perspectives}{

\strong{P1 — Actionable + Real-world.} Balanced segment sizes plus profile
differentiation. Composite of segment balance (SD of proportions, inverted),
minimum segment size, average profile range, count of profiles exceeding
threshold, and LDA accuracy.

\strong{P2 — Maximum Differentiation.} Broad spread across all variables.
Composite of average spread (SD of segment means), median spread, count of
vars with spread > 0.05, even-differentiation count (outlier ratio < 3),
and LDA accuracy.

\strong{P3 — Threshold Hits.} Every segment must be presentable. Reads per-segment
sheets and counts hits (polar diff >= \code{polar_threshold}, profile diff >=
\code{profile_threshold}). Ranks by minimum hits across segments, breaking ties
with average hits. Includes LDA accuracy in composite.

\strong{P4 — Typing Tool Quality.} LDA accuracy weighted heavily, combined with
segment balance and total hits. Answers: which solutions can be deployed as
a reliable typing tool?

\strong{P5 — Polar vs Profile Balance.} Penalizes lopsided hit distributions
within segments. A segment with 40 polar hits and 0 profile hits is hard to
action on. Composite of minimum per-segment polar-profile balance ratio,
average balance ratio, total hits, and LDA accuracy.
}
}

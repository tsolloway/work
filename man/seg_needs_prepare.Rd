% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_needs_prepare.R
\name{seg_needs_prepare}
\alias{seg_needs_prepare}
\title{seg_needs_prepare}
\usage{
seg_needs_prepare(
  seg,
  scale = c("item", "none"),
  center = c("none", "grid"),
  rescale = c("unit", "none")
)
}
\arguments{
\item{seg}{A seg object after \code{\link[=seg_needs_stack]{seg_needs_stack()}}.}

\item{scale}{Character. \code{"item"} (default) z-scores each item across grids.
\code{"none"} leaves the raw values.}

\item{center}{Character. \code{"none"} (default), or \code{"grid"} to also grid-centre
before scaling. Only meaningful for the clustering path.}

\item{rescale}{Character. \code{"unit"} (default) puts the ANALYSIS quantities -
the centred and scaled bases, the person means, the grid intensity - on
0-1 across all items together. It never rewrites the item columns
themselves: those are the survey answers the spec names as its source, and
the spec does its own rescale via \code{Value = "unit 1:3"}. Preparing
variables for analysis and preparing them for the client shell are
separate jobs.}
}
\value{
The seg object with the derived columns on
\code{seg[["data"]][["stacked"]]} and their names in \code{seg[["needs"]][["vars"]]}.
}
\description{
Builds the derived bases on the stacked frame and the person
means that profile them. Writes four families of columns:

\describe{
\item{\code{need01..}}{the survey answers, untouched - the spec rescales these}
\item{\code{needc01..}}{grid-centred - each item minus its own grid's mean}
\item{\code{needs01..}}{item-scaled - each item z-scored across all grids}
\item{\code{pneed01..}}{the respondent's mean per item, on every one of their rows}
\item{\code{ptop01..}}{1 where the respondent gave that item the top code in
any context they saw}
}

plus \code{grid_intensity}, the grid's mean across items.

\strong{Item-scaling is the typing basis and the default.} Without it a
theme built from consensus items wins by default: on the Kadro hearables
data the Frictionless theme took 60\% of grids purely because comfort,
secure fit and battery are the highest-rated items in absolute terms.

\strong{Grid-centring cannot change a theme typing.} It subtracts the same
row constant from every theme score, so the ranking is fixed by
construction - verified to floating-point zero. It is off by default. It
earns its place only for distance-based work, where rater generosity would
otherwise be an axis the clusters split on.

\strong{\code{grid_intensity} is a real variable, not a nuisance.} Rating
generosity stays associated with theme assignment under every
transformation tested (best case eta-squared ~0.08), because intensity and
need-shape are genuinely correlated in the population. Report it rather
than laundering it out of the basis.
}

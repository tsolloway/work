% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_engine.R
\name{bn_engine}
\alias{bn_engine}
\title{Engine to Build and Summarize a Bayesian Network with Optional Layered Complexity}
\usage{
bn_engine(
  df,
  dv,
  ivs,
  dictionary = NULL,
  manual_groups = NULL,
  white_list = NULL,
  black_list = NULL,
  connections_max = 1,
  connections_multiple_boot_threshold = "auto",
  connections_multiple_boot_ratio = 1/4,
  cross_battery_priority = TRUE,
  all_ivs_connect_to_dv = TRUE,
  dv_connection_strength = NULL,
  remove_dv_from_viz_prep = NULL,
  force_dv_connection = NULL,
  only_white_list = FALSE,
  compare_to_naive = TRUE,
  node_label_type = c("both", "variable", "label"),
  n_groups = NULL,
  node_size = 1,
  reachability_max_iter = 10,
  force_white_list_direction = FALSE,
  complexity_boot_n = 100,
  complexity_boot_strength_min = 0.01,
  suppress_bn_warning = FALSE,
  on_exit_detach_igraph = TRUE,
  tool_tip_edge_prefix = NULL,
  seed = 1
)
}
\arguments{
\item{df}{Data frame containing \code{dv} and \code{ivs}.}

\item{dv}{Character scalar. Dependent variable name (column in \code{df}).}

\item{ivs}{Character vector or list of character vectors. Independent variable names.}

\item{dictionary}{Optional mapping or data frame for pretty labels. Passed through
\code{dictionary_from_named_object()}.}

\item{manual_groups}{Optional data frame of node-level group/color/labels for viz.}

\item{white_list}{Optional data frame with columns \code{from}, \code{to} of arcs to force include.}

\item{black_list}{Optional data frame with columns \code{from}, \code{to} of arcs to blacklist.}

\item{connections_max}{Integer (>= 1). Maximum number of parents per non-DV node to allow
after the base structure (1 = base only).}

\item{connections_multiple_boot_threshold}{Numeric in [0,1] or \code{"auto"}.
Threshold for arc strength when adding layers (passed to \code{bn_increase_complexity()}).}

\item{connections_multiple_boot_ratio}{Numeric in (0,1]. When \code{threshold = "auto"},
proportion of top arcs to retain.}

\item{cross_battery_priority}{Logical. If \code{TRUE}, constructs a blacklist to avoid
cross-battery links using \code{ivs \%>\% purrr::map_dfr(make_arcs)}.}

\item{all_ivs_connect_to_dv}{Logical. If \code{TRUE}, forces all IVs (or nodes in base)
to connect to \code{dv} (direction handled downstream).}

\item{dv_connection_strength}{Optional numeric in [0,1]. If provided, uses bootstrap
arc strength cutoff for selecting DV connections; otherwise uses a data-driven cutoff.}

\item{force_dv_connection}{Optional character vector of nodes that must connect to \code{dv}
when \code{all_ivs_connect_to_dv = FALSE}.}

\item{only_white_list}{Logical. If \code{TRUE}, skips TAN backbone and uses only \code{white_list}.}

\item{compare_to_naive}{Logical. Include naive Bayes comparison in summaries.}

\item{node_label_type}{One of \code{c("both","variable","label")}. Label style in viz.}

\item{n_groups}{Optional integer. Target number of communities for viz (if applicable).}

\item{node_size}{Numeric. Node size scaling for viz.}

\item{reachability_max_iter}{Integer. Max passes to enforce path reachability to \code{dv}.}

\item{force_white_list_direction}{Logical (default \code{FALSE}). If \code{TRUE},
user-supplied \code{white_list} arc directions are preserved across the entire
pipeline: forced back over \code{tree.bayes()}'s tree-rooted orientation,
skipped during the intermediary re-orient pass, and excluded from reversal
during reachability enforcement. A warning is emitted if any node remains
unreachable as a result.}

\item{complexity_boot_n}{Integer (default = 100). Number of bootstrap replicates
used when expanding network complexity via \code{bn_increase_complexity()}.
This controls the number of resamples passed to \code{bnlearn::boot.strength()}
for estimating arc strengths before filtering and layer selection.}

\item{complexity_boot_strength_min}{Numeric (default = 0.01). Minimum arc
strength retained when evaluating candidate connections during complexity
expansion. Arcs with estimated strength below this threshold are discarded
prior to direction alignment and layering.}

\item{suppress_bn_warning}{Logical. Suppress learning/fitting warnings in summaries.}

\item{on_exit_detach_igraph}{Logical. Detach igraph on exit via \code{detach_igraph()}.}

\item{tool_tip_edge_prefix}{Optional character string to prepend to edge tooltips (e.g., "MI =").}

\item{seed}{Optional integer. Set seed for reproducible structure learning.}
}
\value{
A list with elements:
\itemize{
\item \code{bn}: the learned \code{bnlearn} network.
\item \code{fit}: the fitted parameters (\code{bn.fit}).
\item \code{viz_prep}: visualization-prep object from \code{bn_to_netviz_prep()}.
\item \code{summary}: model summary list from \code{bn_summary_statistics()}.
\item \code{results_layers} (optional): list with two components when layering was used:
\itemize{
\item \code{layers}: list of per-layer results (bn, fit, viz_prep, summary).
\item \code{summary}: combined data frame of per-layer model summaries.
}
\item \code{meta}: list with metadata (e.g., \code{analysis = "bn_model_single"}).
}
}
\description{
Builds a Bayesian network starting from a Tree-Augmented Naive (TAN)-like
backbone (or a provided whitelist), optionally adds directed connections
to the dependent variable (DV), and can layer in additional parent
connections up to \code{connections_max}. Returns the network, fitted model,
visualization prep, and model summary (optionally with per-layer summaries).
}
\details{
\strong{Workflow}
\enumerate{
\item Create base arcs via \code{bnlearn::tree.bayes()} unless \code{only_white_list = TRUE}.
\item Add DV connections either to all IVs or via selection/bootstrapping.
\item Ensure IVs have reachability to DV via \code{bn_ensure_reachability()}.
\item Fit with \code{bnlearn::bn.fit(method = "bayes")}.
\item Optionally add layers of additional parents up to \code{connections_max} using
\code{bn_increase_complexity()} + bootstrapped arc strengths.
\item Prepare network visualization with \code{bn_to_netviz_prep()}.
}

All graphs are initialized with \strong{all variables} in \code{c(dv, ivs)} to prevent
mismatches between the network nodes and the data frame columns.

This function depends on helper functions in your package:
\code{make_arcs()}, \code{bn_ensure_reachability()}, \code{bn_to_netviz_prep()},
\code{bn_community_color()}, \code{bn_increase_complexity()}, \code{bn_summary_statistics()},
and \code{detach_igraph()}.
}
\examples{
\dontrun{
# Example with iris: DV = Species, IVs = numeric columns
library(bnlearn)
library(dplyr)
df <- iris
dv <- "Species"
ivs <- names(df)[names(df) != dv]

out <- bn_engine(
  df = df,
  dv = dv,
  ivs = ivs,
  connections_max = 1,
  compare_to_naive = TRUE,
  seed = 1
)

out[["bn"]]
out[["summary"]][["model"]]
}

}

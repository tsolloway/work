% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_shell_add_crosstab.R
\name{seg_shell_add_crosstab}
\alias{seg_shell_add_crosstab}
\title{seg_shell_add_crosstab}
\usage{
seg_shell_add_crosstab(
  seg,
  solution,
  variables,
  file_location = NULL,
  sheet_name = "Crosstabs",
  quietly = TRUE
)
}
\arguments{
\item{seg}{A seg object with \code{seg$data$with_solutions} populated.}

\item{solution}{Character. The solution whose workbook will receive the new
sheet. Used to resolve \code{file_location} when \code{file_location} is \code{NULL}.}

\item{variables}{Character vector of column names in
\code{seg$data$with_solutions}. Every unique pair (combinations of 2) is
crosstabbed and written as its own block.}

\item{file_location}{Character. Path to the solution Excel file. Defaults to
\verb{\{seg$paths$folders$solution\}/\{solution\}/Solution - \{solution\}.xlsx}.}

\item{sheet_name}{Character. Sheet name (default \code{"Crosstabs"}). If a sheet
with this name already exists it is removed and re-added.}

\item{quietly}{Logical. If \code{TRUE} (default), suppress console feedback other
than the final write-confirmation message.}
}
\value{
The seg object (invisibly).
}
\description{
Appends a \code{Crosstabs} sheet to a solution workbook containing a
pairwise crosstab block for every unique pair of solution columns in
\code{variables}. Each block has three vertically stacked panels — raw counts,
row percentages, and column percentages — and blocks are arranged
side-by-side across the sheet. Styling (border weight, font size, header
fill, alignment) matches \code{\link[=seg_write_shell]{seg_write_shell()}} conventions.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_shell_add_crosstab.R
\name{.crosstab_resolve_path}
\alias{.crosstab_resolve_path}
\title{Locate the \verb{Solution - \{solution\}.xlsx} workbook. First tries the
conventional path (\code{{seg$paths$folders$solution}/{solution}/...}), then
falls back to a recursive search under the solutions folder so the function
works regardless of whether solutions are organised by solution name or by
family folder. Mirrors the fallback logic in \code{\link[=seg_describe_solutions]{seg_describe_solutions()}}.}
\usage{
.crosstab_resolve_path(seg, solution)
}
\description{
Locate the \verb{Solution - \{solution\}.xlsx} workbook. First tries the
conventional path (\code{{seg$paths$folders$solution}/{solution}/...}), then
falls back to a recursive search under the solutions folder so the function
works regardless of whether solutions are organised by solution name or by
family folder. Mirrors the fallback logic in \code{\link[=seg_describe_solutions]{seg_describe_solutions()}}.
}
\keyword{internal}

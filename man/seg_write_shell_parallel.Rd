% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_write_shell_parallel.R
\name{seg_write_shell_parallel}
\alias{seg_write_shell_parallel}
\title{seg_write_shell_parallel}
\usage{
seg_write_shell_parallel(
  seg,
  solution_vars = NULL,
  where = rep(NA, length(solution_vars)),
  strategy = c("multisession", "multicore", "sequential", "cluster"),
  workers = NULL,
  add_key = TRUE,
  label_width = 75,
  hide_pvalue = FALSE,
  truncate = c("no", "yes", "both"),
  truncate_polar_threshold = 0.15,
  truncate_profile_threshold = 0.1,
  version = c("traditional", "both"),
  do_seg_bw = TRUE,
  do_italic = TRUE,
  switched_polars = FALSE,
  setting_polar_threshold = 0.2,
  setting_profile_threshold = 0.15,
  setting_tolerance = 0.05,
  setting_pvalue = 0.1,
  setting_diff = 0.1,
  setting_type = c("diff", "pvalue"),
  setting_color = c("bw", "color"),
  weighted = TRUE,
  verbose = FALSE
)
}
\description{
Writes solution shell workbooks in parallel. Trims the seg
object to only the components needed by \code{\link[=seg_write_shell]{seg_write_shell()}} before
dispatching to workers — drops redundant data copies and clustering
analysis results to stay under the \code{future.globals.maxSize} limit.
}

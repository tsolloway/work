% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_impact_grain_single.r
\name{bn_impact_grain_single}
\alias{bn_impact_grain_single}
\title{Estimate IV Impacts Across a Bayesian Network using gRain Inference}
\usage{
bn_impact_grain_single(
  obj,
  df,
  ivs = NULL,
  dv = NULL,
  community_assignment = NULL,
  n_boot = 1,
  return_dv_estimate = FALSE,
  use_parallel = FALSE,
  do_community = FALSE
)
}
\arguments{
\item{obj}{A fitted Bayesian network object or a list from \code{bn_engine()}. If
the latter is provided and \code{ivs} is \code{NULL}, the IVs will be inferred from
the network arcs or communities.}

\item{df}{Data frame used to fit or query the Bayesian network. Must contain
the DV and all IV columns.}

\item{ivs}{Optional character vector or named list of IVs to evaluate. If
\code{NULL}, the IVs are inferred automatically when \code{bn} includes metadata.}

\item{dv}{Optional character vector or named list of DVs to evaluate. If
\code{NULL}, the DV is inferred automatically when \code{bn} includes metadata.}

\item{community_assignment}{Optional data frame or list specifying
node-to-community mappings, typically derived from the visualization
preparation object (\code{viz_prep}) returned by \code{\link[=bn_engine]{bn_engine()}}. Must contain at
least the columns \code{id} (variable names) and \code{community_name} (group
identifiers).

When \code{do_community = TRUE}, this argument determines how IVs are grouped
into communities prior to impact estimation. If omitted, the function
automatically searches within \code{obj} for a nested element containing
community information (e.g., \code{viz_prep$nodes},
\code{community_viz_prep$nodes}, or \code{attribute_viz_prep}).

In practice, \code{community_assignment} can be provided in any of the following
forms:
\itemize{
\item A \code{data.frame} or \code{tibble} with columns \code{id} and \code{community_name}.
\item A nested list containing a component named \code{"nodes"} that includes
the same columns.
\item The full visualization prep object from \code{\link[=bn_engine]{bn_engine()}}, from which
communities are automatically extracted.
}

This flexibility allows the function to aggregate IVs by their detected or
manually assigned communities without requiring explicit user input when
working with standardized outputs from the BN engine pipeline.}

\item{n_boot}{Integer. Number of bootstrap replicates. When \code{n_boot = 1},
performs a single estimation without bootstrapping.}

\item{return_dv_estimate}{Logical. If \code{TRUE}, includes the estimated
probability of the DV when IV(s) are set to their maximum observed levels.}

\item{use_parallel}{Logical. If \code{TRUE}, runs bootstrapping across multiple
cores via \code{future}/\code{furrr}. (Windows users should use
\code{plan(multisession)}.)}

\item{do_community}{Logical. If \code{TRUE}, aggregates IVs by their
\code{community_name} groups in the visualization prep object and computes
impact estimates per community instead of per individual IV.

\deqn{ P(DV \mid IV_{max}) - P(DV \mid IV_{min}) }

These estimates can be aggregated, bootstrapped, and normalized into an
index (\code{index}), representing each IV’s proportional effect size relative to
the mean absolute effect.

If \code{bn} includes metadata from \code{\link[=bn_engine]{bn_engine()}}, IVs and the DV are automatically
extracted from the network structure. When \code{do_community = TRUE}, the function
aggregates by \code{community_name} before estimation.

Parallelization is supported via \code{furrr} and \code{future}. Required packages are
automatically loaded within workers.}
}
\value{
A tibble with columns:
\itemize{
\item \code{dv} — Dependent variable name.
\item \code{iv} — Independent variable or community name.
\item \code{estimate} — Mean conditional probability difference.
\item \code{estimate_abs} — Absolute value of \code{estimate}.
\item \code{index} — Relative impact index scaled to mean = 100.
\item \code{dv_estimate} — (Optional) Estimated DV probability at IV max.
}
}
\description{
Computes the relative influence of each independent variable (IV) — or IV
community — on a dependent variable (DV) using conditional probability
differences from \code{gRain} inference. Optionally bootstraps the estimation and
supports both sequential and parallel computation.
}
\section{Community-Level Estimation}{

When \code{do_community = TRUE}, the function converts the
\code{community_assignment} input into a named list mapping community names to
their member IVs. Each community is then treated as a single "super-IV" for
conditional inference:

\deqn{ P(DV \mid IV_{group,max}) - P(DV \mid IV_{group,min}) }

If \code{community_assignment} is not provided and cannot be located within
\code{obj}, an error is thrown. This ensures that every community-level analysis
uses an explicit or discoverable mapping between variables and groups.
}

\examples{
\dontrun{
library(bnlearn)
library(gRain)
library(work)

df <- bnlearn::learning.test
bn <- bnlearn::hc(df)
fit <- bnlearn::bn.fit(bn, df)

# Estimate IV impacts (variable-level)
work::bn_impact_grain_single(
  obj = fit,
  df = df,
  ivs = c("A", "C"),
  n_boot = 20,
  return_dv_estimate = TRUE
)

# Community-level impacts using bn_engine() output
result <- work::bn_engine(df = iris, dv = "Species", ivs = names(iris)[1:4])
work::bn_impact_grain_single(result, df = iris, do_community = TRUE)

# Example with custom community_assignment
custom_comm <- tibble::tibble(
  id = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width"),
  community_name = c("Sepal", "Sepal", "Petal", "Petal")
)
work::bn_impact_grain_single(result, df = iris, do_community = TRUE, community_assignment = custom_comm)
}

}
\seealso{
\code{\link[=bn_impact_grain_engine]{bn_impact_grain_engine()}}, \code{\link[=bn_engine]{bn_engine()}}, \code{\link[gRain:querygrain]{gRain::querygrain()}}
}

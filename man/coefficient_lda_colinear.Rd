% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coefficient_lda_colinear.R
\name{coefficient_lda_colinear}
\alias{coefficient_lda_colinear}
\title{coefficient_lda_colinear}
\usage{
coefficient_lda_colinear(fit)
}
\arguments{
\item{fit}{A fitted \code{MASS::lda} object.}
}
\value{
A tibble with column \code{variable} (first row \code{"constant"},
remaining rows = predictor names) and one numeric column per segment
named \verb{seg_<level>}. Output shape matches \code{\link[=coefficient_lda]{coefficient_lda()}}.
}
\description{
Build the per-segment linear discriminant coefficient table
directly from a fitted \code{MASS::lda} object's \code{scaling} matrix. Output
shape matches \code{\link[=coefficient_lda]{coefficient_lda()}} exactly (same column order, same
\code{variable} column with a \code{constant} row), so the typing tool can consume
either function interchangeably.
}
\details{
Uses MASS::lda's pre-computed scaling — the SVD-based
discriminant directions — instead of inverting the within-class
covariance matrix. Mathematically equivalent to \code{\link[=coefficient_lda]{coefficient_lda()}}
for classification (the linear discriminant boundaries are identical;
the two formulations differ only by a class-invariant offset that
cancels under \code{argmax_k(score_k)}).

\strong{Use this} when:
\itemize{
\item \code{coefficient_lda()} errors with a "system is computationally singular"
message,
\item your LDA inputs include perfectly or near-perfectly collinear
predictors,
\item or sample size is small relative to the number of predictors.
}

MASS::lda computes the scaling matrix via SVD with rank reduction,
so it tolerates rank-deficient inputs that the closed-form
\code{Sigma_W^{-1}} inversion in \code{\link[=coefficient_lda]{coefficient_lda()}} cannot handle.

Per-segment coefficients are computed as
\deqn{\beta_k = L L^\top (\mu_k - \bar\mu)}
where \code{L = fit$scaling} and \verb{\\bar\\mu} is the prior-weighted grand mean.
Per-segment constants are
\deqn{const_k = \log \pi_k - \tfrac{1}{2}\| L^\top(\mu_k - \bar\mu) \|^2 - \bar\mu^\top \beta_k.}
These produce identical class predictions to \code{\link[=coefficient_lda]{coefficient_lda()}};
numeric coefficient values may differ but the typing tool's downstream
\code{argmax(score_k)} lookup is unchanged.
}
\seealso{
\code{\link[=coefficient_lda]{coefficient_lda()}} — the closed-form \code{Sigma_W^{-1}} version.
Faster when inputs are well-conditioned, but errors on collinearity.
}

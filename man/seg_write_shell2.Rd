% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_write_shell2.R
\name{seg_write_shell2}
\alias{seg_write_shell2}
\title{seg_write_shell2}
\usage{
seg_write_shell2(
  seg,
  solution_var,
  key = NULL,
  add_key = FALSE,
  label_width = 75,
  hide_pvalue = FALSE,
  truncate = FALSE,
  truncate_polar_threshold = 0.15,
  truncate_profile_threshold = 0.1,
  var_weight = NULL,
  use_weight = TRUE,
  version = c("traditional", "both"),
  do_seg_bw = TRUE,
  do_italic = TRUE,
  switched_polars = FALSE,
  setting_polar_threshold = 0.2,
  setting_profile_threshold = 0.15,
  setting_tolerance = 0.05,
  setting_pvalue = 0.1,
  setting_diff = 0.1,
  setting_type = c("diff", "pvalue"),
  setting_color = c("bw", "color"),
  where = NULL,
  verbose = FALSE
)
}
\arguments{
\item{seg}{A seg object with data, spec, and solutions populated.}

\item{solution_var}{Character. Name of the segment assignment column.}

\item{key}{Named character vector mapping segment numbers to labels, or
\code{NULL} for auto-numbering.}

\item{add_key}{Logical. If \code{TRUE}, appends a key sheet (default: \code{FALSE}).}

\item{label_width}{Numeric. Column width for label columns (default: \code{75}).}

\item{hide_pvalue}{Logical. If \code{TRUE}, hides p-value columns (default:
\code{FALSE}).}

\item{truncate}{Logical. If \code{TRUE}, removes non-discriminating variables
(default: \code{FALSE}).}

\item{truncate_polar_threshold}{Numeric. Min diff to keep a polar variable
when truncating (default: \code{0.15}).}

\item{truncate_profile_threshold}{Numeric. Min diff to keep a profile
variable when truncating (default: \code{0.1}).}

\item{var_weight}{Character. Weight variable name, or \code{NULL}.}

\item{use_weight}{Logical. If \code{TRUE} (default), applies weight if available.}

\item{version}{Character. Output version: \code{"traditional"} or \code{"both"}.}

\item{do_seg_bw}{Logical. Include black/white formatted sheet (default:
\code{TRUE}).}

\item{do_italic}{Logical. Italicize non-significant values (default: \code{TRUE}).}

\item{switched_polars}{Logical. If \code{TRUE}, reverses polar direction (default:
\code{FALSE}).}

\item{setting_polar_threshold}{Numeric. Polar threshold for conditional
formatting (default: \code{0.2}).}

\item{setting_profile_threshold}{Numeric. Profile threshold for conditional
formatting (default: \code{0.15}).}

\item{setting_tolerance}{Numeric. Tolerance band around thresholds (default:
\code{0.05}).}

\item{setting_pvalue}{Numeric. P-value significance cutoff (default: \code{0.1}).}

\item{setting_diff}{Numeric. Diff-from-total threshold (default: \code{0.1}).}

\item{setting_type}{Character. Significance method: \code{"diff"} or \code{"pvalue"}.}

\item{setting_color}{Character. Color scheme: \code{"bw"} or \code{"color"}.}

\item{where}{Character. Output directory. Defaults to the solution folder.}

\item{verbose}{Logical. Print progress messages (default: \code{FALSE}).}
}
\value{
Invisibly returns \code{NULL}. Writes the solution workbook to disk.
}
\description{
Performance-optimized version of \code{\link[=seg_write_shell]{seg_write_shell()}}. Batches
\code{addStyle} calls to reduce S4 dispatch overhead. Produces identical output.
}

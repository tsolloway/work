% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_impact_write.R
\name{bn_impact_write}
\alias{bn_impact_write}
\title{bn_impact_write}
\usage{
bn_impact_write(
  bn_impact_result,
  file_name = NULL,
  sub_title = NULL,
  title = NULL,
  variable_width = 20,
  community_width = 20,
  label_width = "auto",
  wb_type = c("standard", "dynamic"),
  add_simple_simulator = FALSE,
  bn_obj = NULL,
  dictionary = NULL,
  path = "."
)
}
\arguments{
\item{bn_impact_result}{The output of \code{bn_impact()}.}

\item{file_name}{Character. Prefix for output file name. File is saved as
\code{{file_name} - Network Drivers of {dv}.xlsx}. If NULL, inherits from
\code{sub_title}. If both are NULL, file is saved as
\code{Network Drivers of {dv}.xlsx}.}

\item{sub_title}{Character. Subtitle text (e.g. project name). If NULL,
inherits from \code{file_name}. Default NULL.}

\item{title}{Character. Title displayed in the sheet header. If NULL,
defaults to \code{"Network Drivers of {dv}"} using the DV display name.}

\item{variable_width}{Column width for the Variable column (default 20).}

\item{label_width}{Column width for the Label column (default \code{"auto"}).}

\item{wb_type}{Character. Workbook type: \code{"standard"} (default) or
\code{"dynamic"}.}

\item{add_simple_simulator}{Logical. If TRUE, adds a Simulator sheet with
interactive dropdowns for exploring conditional probability distributions.
Requires \code{bn_obj} to be provided. Default FALSE.}

\item{bn_obj}{The BN subgroups object (named list with \code{fit} and
\code{meta} elements), as produced by \code{bn_finalize_network()}.
Required when \code{add_simple_simulator = TRUE}.}

\item{dictionary}{Optional. A data frame or named object for variable labels.
Passed to the simulator for labeling target variables.}

\item{path}{Directory to write workbook to (default \code{"."}).}
}
\value{
Workbook object (invisibly).
}
\description{
Takes the output of \code{bn_impact()} and writes a formatted
Excel workbook. Reads metadata (type, index_by, subgroups) from the result
object.
}

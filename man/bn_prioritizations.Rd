% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_prioritizations.R
\name{bn_prioritizations}
\alias{bn_prioritizations}
\title{bn_prioritizations}
\usage{
bn_prioritizations(
  obj,
  df,
  dictionary = NULL,
  dv = NULL,
  ivs = NULL,
  ivs_excluded = NULL,
  process_subgroups = TRUE,
  community_assignment = NULL,
  lift = 0.1,
  min_base_for_boot = 75,
  dv_metric = c("mean", "top_box"),
  impact_shift_type = c("proportional", "absolute"),
  impact_result = NULL,
  threshold = 0.01,
  max_rounds = NULL,
  noise_tail = 1/3,
  n_boot_final = 100,
  sig_threshold = 0.05,
  marginal_threshold = 0.1,
  brand = NULL,
  brand_names = NULL,
  weight = NULL,
  use_parallel = TRUE,
  include_maximum_lift_deprecated = FALSE,
  seed = 1
)
}
\arguments{
\item{obj}{A Bayesian network object. Accepts the full output of
\code{bn_finalize_network()} (with \code{bn_subgroups}), a single
subgroup element, or a bare \code{bnlearn::bn.fit} object.}

\item{df}{Data frame containing the DV, IV, and optionally brand/weight
columns.}

\item{dictionary}{Optional. Dictionary for variable labels.}

\item{dv}{Character. Dependent variable name. If NULL, auto-detected from
\code{obj$meta$dv}.}

\item{ivs}{Character vector or NULL. Independent variable names. If NULL,
auto-detected from \code{obj$meta$ivs}.}

\item{ivs_excluded}{Character vector or NULL. Variables to exclude from
the analysis. Default NULL.}

\item{process_subgroups}{Logical. If TRUE and \code{obj} contains
\code{bn_subgroups}, iterate over each subgroup (filtering \code{df}
to rows where \code{df[[subgroup_name]] == 1}). If FALSE, compute once
on the full data. Default TRUE.}

\item{community_assignment}{Optional. Community assignment node table (e.g.,
\code{bn$viz_prep$attribute_viz_prep$nodes}) with \code{id} and
\code{community_name} columns. When provided, a \code{community} column
is added to each result tibble after \code{variable}.}

\item{lift}{Numeric. Distribution shift for lift strategy. Default 0.10.}

\item{min_base_for_boot}{Integer. Minimum sample size to run bootstrap
p-values and to include a brand-subgroup slice as a task. Default 75.}

\item{dv_metric}{Character. \code{"mean"} (default) or \code{"top_box"}.}

\item{impact_shift_type}{Character. How \code{lift} is interpreted when
shifting IV distributions: \code{"proportional"} (default) or
\code{"absolute"}.}

\item{impact_result}{Optional. Output of \code{bn_impact()} passed through
to seed greedy round 1 ordering.}

\item{threshold}{Numeric or NULL. Early stopping threshold. Default 0.01.}

\item{max_rounds}{Integer or NULL. Maximum priority rounds. Default NULL.}

\item{noise_tail}{Numeric. Fraction of tail steps for noise floor.
Default 1/3.}

\item{n_boot_final}{Integer. Bootstrap replicates for p-values.
Default 100.}

\item{sig_threshold}{Numeric. P-value threshold for the "significant"
colour band used by downstream writers. Stored in \code{meta} so
\code{bn_prioritize_write()} and \code{bn_report()} can apply a
consistent threshold. Default 0.05.}

\item{marginal_threshold}{Numeric. P-value threshold for the "marginal"
colour band used by downstream writers. Default 0.10.}

\item{brand}{Character or NULL. Column name in \code{df} containing brand
labels. When provided, lift is computed separately for each brand using
brand-filtered data. Default NULL.}

\item{brand_names}{Character vector or NULL. When provided, only compute
brand-specific lift for these brand levels. Default NULL (all brands).}

\item{weight}{Character or NULL. Column name in \code{df} containing
observation weights. When provided, adds weighted lift variants.
Default NULL.}

\item{use_parallel}{Logical. Parallelize candidate evaluation. Default TRUE.}

\item{include_maximum_lift_deprecated}{Logical. When TRUE, run an
additional Maximum Lift analysis with no baseline comparison
(cumulative_gain == dv_estimate). The result is stored under
\code{greedy_max_deprecated} and surfaces in the writer/report as the
"Maximum Lift (Deprecated)" strategy. Default FALSE.}

\item{seed}{Integer. Random seed. Default 1.}
}
\value{
A list with:
\describe{
\item{greedy_max}{Greedy max results. A tibble (single subgroup) or
named list of tibbles (per subgroup).}
\item{greedy_lift}{Greedy lift results (unweighted).}
\item{greedy_lift_weighted}{Greedy lift results (weighted). NULL if
\code{weight} is NULL.}
\item{greedy_lift_brand}{Named list of per-brand lift results. NULL if
\code{brand} is NULL. Each element is a tibble (single subgroup) or
named list of tibbles (per subgroup).}
\item{greedy_lift_brand_weighted}{Named list of per-brand weighted lift
results. NULL if \code{brand} or \code{weight} is NULL.}
\item{meta}{Shared metadata: dv, subgroups, brand, brand_names, weight,
lift.}
}
}
\description{
Convenience wrapper around \code{bn_prioritize()} that runs
multiple strategy/weight/brand variants and returns them in a single list.
Runs greedy max, greedy lift (unweighted), greedy lift (weighted if
\code{weight} is provided), and per-brand lift (if \code{brand} is
provided). When \code{process_subgroups = TRUE}, each variant is computed
for every subgroup.
}
\seealso{
\code{\link[=bn_prioritize]{bn_prioritize()}}, \code{\link[=bn_impact]{bn_impact()}}, \code{\link[=bn_impacts]{bn_impacts()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seg_build_with_solutions.R
\name{seg_build_with_solutions}
\alias{seg_build_with_solutions}
\title{seg_build_with_solutions}
\usage{
seg_build_with_solutions(seg, resp_id_name = NULL)
}
\arguments{
\item{seg}{A seg object with \code{seg$solutions$analysis} populated.}

\item{resp_id_name}{Character or \code{NULL}. Respondent ID column name in the
base data. If \code{NULL}, auto-detected via \code{\link[=get_resp_id_name]{get_resp_id_name()}}.}
}
\value{
The seg object with \code{seg$data$with_solutions} rebuilt.
}
\description{
Rebuilds \code{seg$data$with_solutions} by joining the base data
(\code{with_solutions} or \code{with_shell}) with all segment assignment columns
stored in the per-family \code{df_solution} list-columns. This is the inverse of
\code{\link[=seg_bind_summary_tables]{seg_bind_summary_tables()}} — where that function builds the lightweight
global summary, this function builds the wide respondent-level dataset.

The function is used internally by the clustering orchestrators after new
solutions are added, but can also be called standalone to rebuild the data
layer from the current solution state.
}

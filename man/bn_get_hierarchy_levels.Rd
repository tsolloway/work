% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bn_get_hierarchy_levels.R
\name{bn_get_hierarchy_levels}
\alias{bn_get_hierarchy_levels}
\title{bn_get_hierarchy_levels}
\usage{
bn_get_hierarchy_levels(obj, root_nodes = NULL)
}
\arguments{
\item{obj}{An \code{attribute_viz_prep}, \code{viz_prep}, or \code{bn} engine result.}

\item{root_nodes}{Optional character vector forcing a specific root set. If
\code{NULL} (default) any node with no incoming arcs is treated as a root.}
}
\value{
A tibble with columns \code{id} and \code{level}.
}
\description{
Computes hierarchy levels for every node in a Bayesian network so they can
be passed as a \code{level} column to \code{visNetwork::visHierarchicalLayout()}.
Roots (nodes with no incoming arcs) get level 0; every other node's level
is \code{max(parent_level) + 1}. This produces a clean top-down layout even when
the underlying DAG isn't a strict tree.

Accepts an \code{attribute_viz_prep} (with \code{nodes}/\code{edges}), a \code{viz_prep} (with
\code{attribute_viz_prep} nested), or a full \code{bn} engine result.
}
